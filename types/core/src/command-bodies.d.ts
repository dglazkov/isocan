import type { SlashCommand } from "./commands.js";
/** The complete commands isocan ships with, including the instructions an agent follows.
 * Pure construction lets menu-only consumers leave these bodies in the deferred full registry. */
export declare const DEFAULT_COMMANDS: SlashCommand[];
