/** An authored long read with useful navigation and local saved passages, without external assets. */
export declare const html: string;
/** The editorial system prioritizes measure and section rhythm rather than application density. */
export declare const design: string;
