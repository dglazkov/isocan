/** A specific synthetic workshop offer and a complete local reservation, with no invented social proof. */
export declare const html: string;
/** The offer's evidence and visual choices are explicit authored facts, not inferred customer proof. */
export declare const design: string;
