/** Complete local receiving workflow; all suppliers and stock are authored synthetic examples. */
export declare const html: string;
/** Authored hierarchy and reusable treatments accompany the runnable bytes. */
export declare const design: string;
