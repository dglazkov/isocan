import { type CanvasSnapshotResponse } from "../../core/src/index.js";
/** Refuse a legacy or read-only destination with the guidance above. */
export declare function assertGroupDestination(state: CanvasSnapshotResponse, edit: boolean): CanvasSnapshotResponse;
