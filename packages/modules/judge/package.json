{
  "name": "@isocan/judge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Judge: the calibration corpus — every screen a wireframe flow drew, its round-1 P(yes), and what the person running the reader then did with it",
  "exports": {
    "./core": "./src/core.ts",
    "./web": "./src/web.tsx",
    "./cli": "./src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@isocan/core": "*"
  }
}
