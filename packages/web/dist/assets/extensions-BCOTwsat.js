import{am as d,an as h,ao as m}from"./index-DH9rEvjW.js";const u="role",p="tool";function w(){return{[u]:p}}function f(n){return n.properties[u]===p}const l=["broom","wand","check","star","tag","list","eye","bolt","clock","flag","link","note"],c=24,b=["select","hand","zoom","pen","text","comment","add","isocan"];function g(n){return n.toLowerCase().replace(/[^a-z0-9]/g,"")}function y(n,a){let o;try{o=JSON.parse(n)}catch{return{problem:"not JSON — a tool is a small JSON file, see `isocan tool add --help`."}}if(typeof o!="object"||o===null||Array.isArray(o))return{problem:"not a JSON object — a tool is one object, not a list."};const e=o;if(e.kind!=="tool")return{problem:e.kind===void 0?'no "kind" — a tool declares `"kind": "tool"`.':`"kind" is ${JSON.stringify(e.kind)}; only "tool" is built (panels are stage 3).`};const t=typeof e.label=="string"?e.label.trim():"";if(!t)return{problem:'no "label" — a button with no words on it is not a button.'};if(t.length>c)return{problem:`"label" is ${t.length} characters; a rail button holds ${c}.`};if(b.includes(g(t)))return{problem:`"${t}" is one of the app's own tools. An extension wears its own name, because a control that looks exactly like isocan is where somebody would put a convincing "sign in to continue".`};const r=e.icon;if(typeof r!="string"||!l.includes(r))return{problem:`"icon" must be one of the named set — ${l.join(", ")} — and not an image of your own, because an icon is a place anything at all could be painted.`};const i=typeof e.does=="string"?e.does.trim():"";if(!i)return{problem:'no "does" — a tool is a button plus the ask it makes.'};const s=d(i);return s?h.test(s.name)?m(a,s.name)?{tool:{kind:"tool",label:t,icon:r,does:i}}:{problem:`no command called "/${s.name}" here. A tool may only ask for what a person could ask for, so it can name a command this canvas has and no other.`}:{problem:`"/${s.name}" is not a command name — lowercase letters, digits and dashes.`}:{problem:`"does" is ${JSON.stringify(i)}; it must be a slash command, like "/format".`}}function O(n,a){const o=d(n.does),e=o?m(a,o.name):null,t=[];return e?(e.local?t.push(`answers in the app: ${e.description}`):(t.push(`posts a comment as you, asking an agent to: ${e.description}`),t.push("whatever that agent then does is attributed to it, and undoable per actor")),o?.args&&t.push(`always with the same words after it: "${o.args}"`),t.push(`the command is ${e.source==="built-in"?"one isocan ships":e.source==="home"?"this home's own":"carried by a loaded module"}`),t):(t.push(`asks for /${o?.name??"?"}, which this canvas does not have`),t)}function E(n){return Object.values(n.items).filter(f).sort((a,o)=>a.createdAt<o.createdAt?-1:a.createdAt>o.createdAt?1:a.id.localeCompare(o.id))}export{l as E,c as L,p as T,O as a,w as b,f as i,y as r,E as t};
