{
  "name": "isocan",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "An isomorphic canvas: one 2.5D surface, driven from a web app and a terminal",
  "bin": {
    "isocan": "packages/cli/bin/isocan.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev -w @isocan/server\" \"npm run dev -w @isocan/web\"",
    "build": "npm run build -w @isocan/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "release": "node scripts/release.mjs",
    "prepare": "node scripts/prepare.mjs"
  },
  "//": "The runtime deps the CLI needs are declared HERE as well as in packages/cli, so that `npm i -g github:dglazkov/isocan#release` (no publishing) resolves them: a git install installs the root package only. The workspaces themselves are deliberately NOT dependencies — bin/workspace-loader.mjs resolves them by path, because link dependencies break every reinstall. `workspaces` and `prepare` are why installs come from the release branch and not from here: npm's git installer empties the install directory for any package it thinks it must build first (#47, scripts/release.mjs).",
  "dependencies": {
    "commander": "^13.1.0",
    "fastify": "^5.2.1",
    "nanoid": "^5.0.9",
    "tsx": "^4.19.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
