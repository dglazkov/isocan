{
  "name": "isocan",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "An isomorphic canvas: one 2.5D surface, driven from a web app and a terminal",
  "bin": {
    "isocan": "packages/cli/bin/isocan.js"
  },
  "//": "GENERATED BRANCH — `npm run release` builds it from 0878db5 on main; develop there, not here. No `workspaces` and no scripts, deliberately: npm's git installer treats either as \"needs preparation\", and then installs this package into an empty directory (#47). The built web app is committed here for the same reason — there is no install-time build to make it.",
  "dependencies": {
    "commander": "^13.1.0",
    "fastify": "^5.2.1",
    "nanoid": "^5.0.9",
    "tsx": "^4.19.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "isocan": {
    "commit": "0878db5",
    "builtAt": "2026-08-25T18:24:54-07:00"
  }
}
