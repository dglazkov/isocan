{
  "name": "isocan",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "An isomorphic canvas: one 2.5D surface, driven from a web app and a terminal",
  "bin": {
    "isocan": "packages/cli/bin/isocan.js"
  },
  "exports": {
    ".": {
      "types": "./types/api/src/index.d.ts",
      "default": "./index.mjs"
    },
    "./package.json": "./package.json"
  },
  "//": "GENERATED BRANCH — `npm run release` builds it from 4a03977 on main; develop there, not here. No `workspaces` and no scripts, deliberately: npm's git installer treats either as \"needs preparation\", and then installs this package into an empty directory (#47). The built web app is committed here for the same reason — there is no install-time build to make it.",
  "dependencies": {
    "@types/node": "^22.13.1",
    "commander": "^13.1.0",
    "fastify": "^5.2.1",
    "nanoid": "^5.0.9",
    "tsx": "^4.19.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.68.0",
    "concurrently": "^9.1.2",
    "eslint": "^10.9.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "typescript": "^5.7.3",
    "undici": "^7.29.0",
    "vitest": "^3.0.5"
  },
  "isocan": {
    "commit": "4a03977",
    "builtAt": "2026-09-04T18:10:50-07:00"
  }
}
