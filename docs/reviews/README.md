# Reviews

Standing reviews of this repo, by the personas in
[`.agents/personas/`](../../.agents/personas/) (Claude Code reads the same
files through symlinks in `.claude/agents/`). Each one is a pass by somebody
whose only job is that lens.

| Persona | The lens | Its number |
| --- | --- | --- |
| `accessibility` | Usable by somebody not using it the way you are | contrast, names, targets, alt |
| `architect` | The op vocabulary, boundaries, the isomorphism | core's runtime deps, op types |
| `copy` | The words: labels, errors, tooltips, empty states | greppable copy tells |
| `design-auditor` | Tokens, both themes, the tells of a generated interface | grader checks, colour literals |
| `market-researcher` | What else exists and what to take from it | **none, honestly** |
| `performance` | Whether it still feels fast | largest built chunk |
| `qa-tester` | Whether the tests mean anything | eslint errors |
| `reviewer` | Whether the code says true things about itself | unused exports, undocumented exports — both **ratchets** |

**Eight, and the count is the discipline rather than a target.** The design
says four fit in a head and warns against a persona per concern; the gate it
sets is stricter and is what these pass: *a persona needs a standing number
nobody else is watching.* `market-researcher` has none and says so out loud in
`isocan persona ls` — a lens that cannot state a number is worth keeping and
worth being honest about, not worth a made-up metric.

Two roles were considered and NOT added, for the same reason: **security** (the
jails are real and well-tested, but there is no standing number today, and the
architect's review already covers the boundaries) and **docs** (drift between
what the docs claim and what the code does — which is already, by name, part of
the architect's charter).

They exist because the work moves fast and the things that rot quietly —
a token nobody used, a test that asserts nothing, a dependency that stopped
being justified — are exactly the things nobody notices while shipping.

## How the memory works

Every run **reads before it looks**. The index below is the first thing a
persona opens, so a run in October knows what a run in August already
measured and can say "still true", "fixed", or "worse" instead of rediscovering
it. A finding that keeps reappearing across runs is a finding that needs a test,
not a third mention.

- **Findings** land here as `YYYY-MM-DD-<persona>.md`, and the table below is
  **generated from them** by `scripts/reviews.mjs` — it was hand-kept until
  1 Sep 2026 and had fallen four run-days behind, which meant every run in that
  stretch opened the memory it is supposed to read and found nothing after
  24 Aug. Editing it by hand is editing the wrong file; the pages are the
  source.
- **[lessons.md](lessons.md)** is different and more valuable: the standing list
  of failure modes this codebase has actually produced, each with the guard that
  now catches it. It is append-only in spirit — a lesson leaves only when the
  thing it describes has become impossible.
- **Market research** goes to [`docs/research/`](../research/) instead, which
  already exists for exactly that and has its own index.

## What makes a finding worth writing down

The same bar the `/design-audit` command holds: cite the line, or the measured
value, or the command whose output you pasted. "Improve consistency" is not a
finding. "`--chip-hover` is declared in both themes and referenced nowhere
since 0e51081" is.

Say what is GOOD too, specifically. A review with no green is a review people
stop believing, and it is the half that tells you what not to break.

## The runs

<!-- generated by scripts/reviews.mjs — do not edit between these markers -->

**35 automated runs**, newest first. **13 findings unanswered** — the only thing a run wants from a person — and 8 runs carrying a missed bound.

A missed goal is news; a broken instrument is a failure. `unanswered` is not a
backlog to be cleared for tidiness — it is the row nobody has said `accepted` or
`rejected` on yet, and a finding that keeps reappearing needs a guard rather than
a third mention.

| Date | Persona | Goals | Missed | Unanswered |
| --- | --- | --- | --- | --- |
| [2026-09-03](2026-09-03-accessibility.md) | accessibility | 2/2 held | — | — |
| [2026-09-03](2026-09-03-architect.md) | architect | 2/2 held | — | — |
| [2026-09-03](2026-09-03-copy.md) | copy | 1/1 held | — | — |
| [2026-09-03](2026-09-03-design-auditor.md) | design-auditor | 2/2 held | — | — |
| [2026-09-03](2026-09-03-journeys.md) | journeys | no standing number | — | — |
| [2026-09-03](2026-09-03-market-researcher.md) | market-researcher | no standing number | — | — |
| [2026-09-03](2026-09-03-performance.md) | performance | 0/1 held | **the entry chunk a first visit downloads** — 648435 (was 600420 on 2026-09-02) | 1 |
| [2026-09-03](2026-09-03-qa-tester.md) | qa-tester | 1/1 held | — | — |
| [2026-09-03](2026-09-03-reviewer.md) | reviewer | 0/3 held | **exports nothing outside their own file uses** — 25 (was 0 on 2026-09-02); **exports with no comment above them** — 301 (was 253 on 2026-09-02); **CSS rule bodies copied word for word from elsewhere** — 53 (was 47 on 2026-08-30) | 3 |
| [2026-09-01](2026-09-01-accessibility.md) | accessibility | 2/2 held | — | — |
| [2026-09-01](2026-09-01-architect.md) | architect | 1/2 held | **operations in the vocabulary** — 32 (was 31 on 2026-08-30) | 1 |
| [2026-09-01](2026-09-01-copy.md) | copy | 1/1 held | — | — |
| [2026-09-01](2026-09-01-design-auditor.md) | design-auditor | 2/2 held | — | — |
| [2026-09-01](2026-09-01-journeys.md) | journeys | no standing number | — | — |
| [2026-09-01](2026-09-01-market-researcher.md) | market-researcher | no standing number | — | — |
| [2026-09-01](2026-09-01-performance.md) | performance | 0/1 held | **largest built JavaScript chunk** — 737756 (was 673076 on 2026-08-29) | 1 |
| [2026-09-01](2026-09-01-qa-tester.md) | qa-tester | 1/1 held | — | — |
| [2026-09-01](2026-09-01-reviewer.md) | reviewer | 1/3 held | **exports nothing outside their own file uses** — 139 (was 129 on 2026-08-30); **exports with no comment above them** — 307 (was 277 on 2026-08-30) | 2 |
| [2026-08-31](2026-08-31-accessibility.md) | accessibility | 2/2 held | — | — |
| [2026-08-31](2026-08-31-architect.md) | architect | 1/2 held | **operations in the vocabulary** — 32 (was 31 on 2026-08-30) | 1 |
| [2026-08-31](2026-08-31-copy.md) | copy | 1/1 held | — | — |
| [2026-08-31](2026-08-31-design-auditor.md) | design-auditor | 2/2 held | — | — |
| [2026-08-31](2026-08-31-journeys.md) | journeys | no standing number | — | — |
| [2026-08-31](2026-08-31-market-researcher.md) | market-researcher | no standing number | — | — |
| [2026-08-31](2026-08-31-performance.md) | performance | 0/1 held | **largest built JavaScript chunk** — 713306 (was 673076 on 2026-08-29) | 1 |
| [2026-08-31](2026-08-31-qa-tester.md) | qa-tester | 1/1 held | — | — |
| [2026-08-31](2026-08-31-reviewer.md) | reviewer | 0/3 held | **exports nothing outside their own file uses** — 137 (was 129 on 2026-08-30); **exports with no comment above them** — 304 (was 277 on 2026-08-30); **CSS rule bodies copied word for word from elsewhere** — 49 (was 47 on 2026-08-30) | 3 |
| [2026-08-30](2026-08-30-reviewer.md) | reviewer | 2/2 held | — | — |
| [2026-08-29](2026-08-29-accessibility.md) | accessibility | 2/2 held | — | — |
| [2026-08-29](2026-08-29-architect.md) | architect | 2/2 held | — | — |
| [2026-08-29](2026-08-29-copy.md) | copy | 1/1 held | — | — |
| [2026-08-29](2026-08-29-design-auditor.md) | design-auditor | 2/2 held | — | — |
| [2026-08-29](2026-08-29-market-researcher.md) | market-researcher | no standing number | — | — |
| [2026-08-29](2026-08-29-performance.md) | performance | 1/1 held | — | — |
| [2026-08-29](2026-08-29-qa-tester.md) | qa-tester | 1/1 held | — | — |

<!-- end generated -->

## Before the runs were automated

The three review days below predate `persona-run.mjs`. They are paragraphs
of measured prose rather than a goals table, so nothing generates them and
nothing should overwrite them — they are still the most detailed reading this
codebase has had.

| Date | Persona | What it found |
| --- | --- | --- |
| [2026-08-24b](2026-08-24b-design.md) | design | Third run, second today; every standing finding re-measured against `5c1d1d5`, which landed underneath me mid-review (lesson #7, twice running). **The title-row work is right and measures right**: nine zooms from 149% to 25%, gap above the card **6.00 ± 0.02 screen px at every one** and **0.00 px of movement on select**, and `titlebar.test.ts` / `worldchrome.test.ts` carry the mutations that walked past their own first drafts. But the fix stopped at the class it was about. `.version-badge` is counter-scaled by the same line of `ItemView` and still positioned in WORLD px — `bottom: -9px; right: 14px` — so its gap below the card runs **11.93 screen px at 149% to 1.60 at 20%** on an element that is 27.7×16 in every row of that table, and below **43%** (derived `6 > 14·s`, first seen at 39%) it slides under `.resize-handle-se`, measured 0.53×9.52 px growing to 3.20×7.80 — while the CSS two lines above says every corner belongs to a handle *and that is why the badge sits on the edge*. `worldchrome.test.ts`'s own list names the four handles individually for exactly this reason and omits the badge; three strings fix it. **Focus ring, third run, now with a number per ground**: tabbed 40 times in dark, **26 distinct stops, every one `2px solid rgb(64,89,216)`** — 3.32 on `--ground`, 3.05 on `--panel`, **2.90** on `--card`, **2.50** on `--chip`, against SC 1.4.11's 3:1; a `--focus` token (`#1f3fd0` / `#8fa8ff`) takes it to 6.32–8.41 and is a no-op in light. `--ink-soft` on `--chip` is **4.41 dark / 4.54 light** and is now TWO rules, so it is the token and not the rule: `#868f9e` → `#8891a0` gives 4.53/5.26/6.03. `color-scheme` measured `normal` in all four theme states (third run). **9 of 29 rendered controls under 24×24**, smallest `.attached-chip button` at **13.6×10 = 136px² of a 576px² floor**. Trash button still the 1 of 26 tab stops with no accessible name. New `packages/web/test/scale.test.ts` (green, 10 cases, **10 mutations — 8 caught on the right case, 2 negative controls silent**, including the parser blinded) ratchets what had none: **407 spacing lengths in 25 distinct values**, 106 radius literals in 14 against 10 `var(--radius)`, **8 rules spelling out `border-radius: 8px`**, 170 font-sizes in 17 with five half-pixel. It imposes no 4px grid — nobody chose one — only *do not invent a new step*, as an equality so a fix must be recorded. The contrast probe was WRONG first (sRGB denominator 2.055) and reported 32 findings; self-tested against the repo's own published 5.12/5.15 it reports **9 of 196 distinct text elements over 1,550 samples in four theme states**, 8 of them the one already-ratcheted accent-as-ink cause and the 9th a `:disabled` control. Green: **zero dead tokens** (39 declared, 39 referenced), zero literal colours, the layer scale held across two new components (45 z-index, 29 tokenised, the 16 raw ones all local ordering inside `.item`), the third state stamped before first paint, and the `?` list true in BOTH directions — every key the handlers answer is documented, `Z` included, which is the direction `shortcuts.test.ts` does not check. Copy: zero hits on every greppable `slop.ts` tell; the only matches in the repo are comments arguing against them. |
| [2026-08-24b](2026-08-24b-qa.md) | qa | Third run, second today. **53 mutations, 24 killed as found (45%)** — 27 of 29 survivors now dead, 2 equivalent. The newer the file, the more the test was written from the path rather than from the rule; the pattern is now three runs old. **The two guards written this weekend to prevent vacuous passes were both vacuous.** `worldchrome.test.ts`'s `has rules for every selector it claims to check` — the comment says "so the file cannot pass vacuously" — passed 226/226 with `.item.peeked`'s ring DELETED (a descendant rule kept the list non-empty and the sizing sweep then had nothing to reject) and with `.resize-handle` renamed to `.resize-handle-base` (the four corner rules are superstrings). `titlebar.test.ts`, hours old, passed 3/3 alone with the rule it exists to guard deleted. Both closed with existence-AND-function checks and a boundary-aware matcher; 12 mutations, 12 killed. New **lesson #16**. The same file did not watch `.item-titlebar` at all, so the hop bug the next commit fixed could be restored, both halves, with the web suite green — closed and **proved against the pre-fix CSS**. **`e0dc1b2`'s "floor that was not one" had no guard**: `Math.max(MIN_NAME_ROOM, …)` drew the name through the star at 13%, and the rule lived inline in a component nothing could import (lesson #5, one file along). Extracted to `nameRoom`/`nameFits` in `lib/chrome.ts`; the guard asserts LINEARITY rather than the constants, so a 1px floor dies and retuning does not lie (9 mutations, 9 killed, new **lesson #17**). **`alreadyWritten`'s backwards oplog scan could be truncated to the LAST ENTRY** with `idempotency.test.ts` green — every case retried with nothing in between, while the real shape is a queue and other people writing; the retry is now buried under five of the actor's own ops and under another actor's (new **lesson #18**). **`parseRange`'s four boundary branches were all optional** — `bytes=<size>-` (what `curl -C -` sends when finished), an inverted range, a trailer longer than the file, and `bytes=-0` — with `blobs.test.ts` green; three now die, one is an equivalent mutant named. `size === 0` is unreachable only because three separate doors refuse it, so the reachability argument is now a test. **`mentions.ts`'s word-boundary controls were all ASCII**: `/[\p{L}\p{N}_]/u` → `/[A-Za-z0-9_]/` was green, which makes `سلام@دیون` a mention. Two people with one name (the default case for agents) reach exactly one, silently — pinned, not fixed, because it needs a design answer. **The flake was `restart.test.ts` and it is the copy that never got the fix**: `stdio: "ignore"`, a bare `the other copy never came up`, and a 15s budget nobody re-measured when the global went to 30s — the evidence habit was learned in `daemon-takeover.test.ts` and written down. Now fails in 164ms with the child's stderr. That run also found `expect(root).toMatch(/isocan$/)`, a test asserting the checkout's DIRECTORY NAME. `geometry.test.ts`, landed under me, is the best new test of the week. Lesson #7 recurred a third day (`5c1d1d5` swept this review's files — and said so by name, which is new). |
| [2026-08-24b](2026-08-24b-architecture.md) | architect | Third run, second today; standing findings re-measured, not recalled. **Two green suite runs** (1071 passed, 112 files, 57.6s), typecheck clean, core still one dependency and zero `node:` imports, `applyOperation` exported once and every `canvas.items[…]` write outside the reducer is a read. **N1 fixed, and better than proposed** — `c088f61` normalizes `item.add` fully in the engine so the reducer's own search is a no-op on the way back, with `positionIsMeaningful` moved to core; D8 is the first drift row in three runs closed by the CODE. **The worst finding is measured divergence**: three mime tables in two directions, and `kinds.ts:34` names `application/pdf` — a type the CLI's table cannot produce — so `isocan add report.pdf` is `other` and a browser drop of the same file is `document`, which is exactly what that file's own comment says it exists to prevent. **Second: the door checks who you are exhaustively and what you sent not at all.** `POST /api/ops {"type":"item.move","x":"banana","y":null}` → **200, applied, durably logged**; `Operation` is a TypeScript union and TypeScript is not there at runtime. Bounded today; `extensions.md:60` (`does` may be an `Operation`) and phase 10's IndexedDB queue unbound it. **`architecture.md`'s "Distance to the map" lists the Share dialog and grant routes as unbuilt** — `ShareDialog.tsx`, `http.ts:786/818/876` and `main.ts:1351` shipped in phase 7; `placement.ts`/`fit.ts` still 0 mentions. The retired "on call" presence is still taught in **five** places, one of them `Presence.tsx:19-22` describing a dashed ring the component has no rule for. Judged `extensions.md`: model correct (`role=tool` is invariant 3 exactly, no new op), but `role` is a namespace with no registry, `does` needs runtime validation first, and "Both surfaces" is satisfied by `--prop` — an escape hatch `isocan star` and `isocan design` both declined to accept. `cloud-desk.ts`'s "one writer" is **five** (`:118, :262, :396, :420, :452`), all correct, and the invariant actually held is `denormalize`. Tarball worse again: 396 files / 5.1 MB, **47% docs+tests+infra**, 113 test files inside `packages/`. `npm test`: **44 of 112 files are 97% of the cost** (core's 31 files: 192ms total); CI **9 red of 100 release runs**, every one at `npm test`, and **0 of 9 since the 30s timeout landed**. Found the `worldchrome` allowlist stop watching the titlebar when the rule moved selectors — **closed under me** by the session that owns the file, better than proposed. |
| [2026-08-24](2026-08-24-qa.md) | qa | Second run; standing findings re-checked by running them. **`main` is green again**; the stamp and undo-stack guards hold. **42 mutations on the weekend's three files, only 22 killed as found (52%) against last run's 60/63 on the mature core** — 17 survivors now dead, 3 equivalent. The weekend's code was the weak half. `placement.ts`: `overlaps()`'s ENTIRE y test can become `a.y === b.y` — a box one pixel lower reads as clear — with the core suite green 340/340, because every fixture collides along x; six more survivors covered the clearance, the lattice gap, the tie-break, the distance sort and the give-up path. It matters more now that `item.add` resolves through it inside the reducer. `spatialnav.ts`: **the ⌘Up fix was tested on one arm of a four-arm switch** — the identical regression on Down and on Left left the web suite green 178/178; `ORTHOGONAL_WEIGHT` and centre-vs-corner were also unmeasured. `fit.ts`: a width-only or height-only fit was silently dropped. 34 new cases, each proved by the mutation it was written for; one of my own tests derived its fixture from the constant under test and passed for the wrong reason (new lesson #11). **The flake is named**: 6 more clean runs proved nothing, so I measured the suite under 24x CPU load — **90 tests over 2500ms and 7 failures**, five at the 5s default in `identity.test.ts`, `restart.test.ts` and three MORE cases of `session-identity.test.ts`, whose one named case had just been fixed alone. `vitest.config.ts` now sets 30s with the measurement beside it; same load, **7 → 0**. `grade.mjs --selftest` is real — 9 breaks, 9 caught by name — but **nothing runs it**. Two guards had the silent-zero shape and passed with their parser broken (`oneblock`, `layers`), both closed and proved both ways. `mentions.ts` migrated off the stamped name: the @-menu offered a renamed person under their old name, which reached nobody. Three copies of the `freePort` TOCTOU remain. |
| [2026-08-24](2026-08-24-design.md) | design | Second run; every standing finding re-measured rather than recalled — **nine of nine app findings and five of five marketing findings are still true**, two of them worse (spacing 23 → 32 distinct values, 325 → 403 declarations; `.cursor-chip em` dims white to 3.50:1 over Teal, putting six of seven identity colours under 4.5). The accent ratchet from the baseline held across a whole new component: 0 new offenders, 0 z-index outside the scale. The new `DesignSystemView` is the priority and its own claim is the worst finding: `readableInk` measures every swatch against black-or-white, whose floor over the whole sRGB cube is **4.58:1**, so the badge that exists to catch an unreadable palette cannot ever fail — while `prj_CIhLRcdYrD`'s own `text-muted #858688` is **3.27:1** on its own `surface` and prints 5.8. `checkDesign` is in core, the CLI runs it, the view stops one import short. At the 420×320 `isocan add` gives the item it renders **15,359px into 318px (48 screens)** with the masthead alone taking 88% and the first swatch 191px below the fold; `⇧F` caps at 2,400 (16%). `.ds-prose` renders the same `remarkGfm` markdown with 2 rules where `.md-view` has 12, so a table loses its borders and pushes the view 33px sideways. Three of the view's own labels are under 4.5:1 in **every** theme, all by `opacity` — including the contrast badge itself, printing "4.6" at 3.77:1. New `packages/web/test/dimmed.test.ts` (green, 7 mutations) derives the 0.93 opacity budget from the tokens, proves the 4.58 floor by search, and ratchets the four rules. `scripts/grade.mjs` does not overlap: it grades files, gives marketing 8/8, and cannot see a React view at all. |
| [2026-08-24](2026-08-24-architecture.md) | architect | Second run; standing findings re-checked, not remembered. **Finding 1 is closed with a test.** The substring guard had already cost something: `fit` (`main.ts:2743`, shipped this week on both surfaces), `session move` and `trash empty` were all registered, all absent from the guide, all green — `surface.test.ts` now derives the documented set from inline code spans and a second test freezes the prose regression. **Finding 4 fixed for the named test** by another session mid-review (`3c7825e`), with a better diagnosis than the review proposed — but the class reproduced: of five runs here, four green (1032 tests, 35s) and one red (8 tests, 3 files) during the one run that took 68.8s under a concurrent session. Load, not logic, again. **Finding 5 worse**: `docs/` grew to 1,315 KB of a 5.0 MB tarball nothing reads. Findings 2, 3a–c, 6, 7 and D1–D4 all still true. New: `item.add`'s final position is decided in the reducer and never logged, so `isocan add b.md --at 0,0 --json` reports `0,0` for an item measured at **460,0** — and `nearestFreeSpot` is now frozen into replay semantics (engine.ts:1529 normalizes the anchor but not the collision). `fit` is one gesture and N+1 undo steps on both surfaces, identically. `architecture.md` claims the whole system is mapped and mentions `placement.ts`/`fit.ts` zero times. Judged both proposals: `item.adopt`'s shape cannot be applied purely (the reducer would have to mint version ids) and its inverse is **not** computable as claimed — `invertOperation` returns one op, so adopt needs a new internal op, not the two that exist; counter-shape proposed. `content-origin.md` accepted — split the local half from the hosted half, design blob addressing first, and land a seam-test-shaped guard on `allow-same-origin`. |
| [2026-08-23](2026-08-23-qa.md) | qa | Baseline. 63 mutations, **60 killed** — the reducer, inverses and per-actor undo stacks are genuinely guarded. Three real findings. `110027d` committed a live mutation into `packages/core/src/reducer.ts`, dropping `item.add`'s trash check, so **main is red** (fix in the working tree). Lesson #2's guard defined its own `nameOf` and imported nothing — reintroducing "null is working" left it 10/10 green; the rule now lives in `sessionName` and the test imports it (5 mutations, 5 killed), though seven other call sites still use the stamped name. The reducer's own "every mutation stamps updatedAt/updatedBy" was never checked — dropping `item.move`'s stamp was the one genuine survivor (now 13 mutations, 13 killed). `oneblock`'s count only sees a *second* bare rule: 2 mutations on the 57 classes that lack a canonical block survived the whole suite; partly closed with a derived modifier invariant. A no-op `discardUndoTarget` HANGS the suite instead of failing it — new `undo-stacks.test.ts` (10/10). All 45 skips are the emulator-gated cloud suites, correctly designed and CI-enforced. The daemon-takeover flake did not reproduce in 8 runs or under 3× CPU oversubscription, and the port hypothesis measured 0 collisions in 3,900 allocations; hardened anyway with a per-worker port range and failures that name their cause. |
| [2026-08-23](2026-08-23-design.md) | design | Baseline. The two worst app findings share one root cause: `--accent` is a FILL in dark and the app uses it as ink — 14 rules paint text at 2.5–2.9:1 on graphite, and the focus ring itself is 2.90:1 on nine of nineteen focusable controls (under SC 1.4.11's 3:1) while measuring 6.9–7.8:1 in light. `--accent-text` already exists for this. `color-scheme` is never declared, so scrollbars and native controls stay light in dark mode. Teal and Amber are 4.23/4.24:1 under their white initials. Spacing and radii still have no guard: 23 distinct spacing values (64% off the 4px grid) and 13 literal radii against 11 uses of `--radius`. Added `packages/web/test/accent.test.ts` (green, mutation-tested) to ratchet the 14. Marketing: the focus ring is 1.81:1 inside the closing band, and the crew chips wear four colours `IDENTITY_COLORS` does not contain — one of them the cobalt the product reserves. |
| [2026-08-23](2026-08-23-architecture.md) | architect | Isomorphism holds — every op the web issues, the CLI issues. But `surface.test.ts:49` matches substrings, so `session move` is registered and absent from the guide with the build green; nothing watches the web surface at all. Three computations duplicated across clients, one already divergent (a 5,200-byte file is "5.1 KB" in the CLI and "5 KB" in the files panel). `npm test` failed a different test in 2 of 3 runs. README drift in four places, one self-contradicting ("On call" vs #60). |
