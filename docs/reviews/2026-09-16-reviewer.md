# reviewer — 2026-09-16

Run by `scripts/persona-run.mjs` at `fba4b87`. **Nothing was changed.**

| Goal | Target | Now | Verdict |
| --- | --- | --- | --- |
| exports nothing outside their own file uses | at most 39 | 39 | held |
| exports with no comment above them | at most 253 | 330 (was 253 on 2026-09-02) | **MISSED** |
| CSS rule bodies copied word for word from elsewhere | at most 60 | 58 (was 47 on 2026-08-30) | held |

## Findings

| Finding | Outcome |
| --- | --- |
| exports with no comment above them is 330, past 253 | accepted — fixed, not moved: 238 after 852a1112 documented 93 of them in core; the bound held at 253. |

`unanswered` until somebody writes `accepted` or `rejected`. **After 3 days
an unanswered row fails `npm test`** — the queue can fail, so a correct report
cannot be quietly ignored the way six nights of them were (#197).

---

Read `docs/reviews/README.md` before the next run: a finding that keeps
reappearing is a finding that needs a guard, not a third mention.
