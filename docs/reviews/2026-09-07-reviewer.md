# reviewer — 2026-09-07

Run by `scripts/persona-run.mjs` at `990062e`. **Nothing was changed.**

| Goal | Target | Now | Verdict |
| --- | --- | --- | --- |
| exports nothing outside their own file uses | at most 39 | 39 | held |
| exports with no comment above them | at most 253 | 331 (was 253 on 2026-09-02) | **MISSED** |
| CSS rule bodies copied word for word from elsewhere | at most 47 | 59 (was 47 on 2026-08-30) | **MISSED** |

## Findings

| Finding | Outcome |
| --- | --- |
| exports with no comment above them is 331, past 253 | unanswered |
| CSS rule bodies copied word for word from elsewhere is 59, past 47 | accepted — ten nights at 59 against a goal of 47, and the number has not moved since `test/copied-rules.test.ts` began holding it there on 6 September. Looked at again on 10 September (`node scripts/measure.mjs copied-rules`): the twelve above the goal are the same "two things that agree" the 6 September row describes — six controls sharing what "on" looks like, flex rows and columns — plus the three real copies that row left for a person with the app open. The ratchet is the enforcement. What is left is the persona's bound: at 47 it files this same question every night, and a goal below the truth answered again every three days is the treadmill `README.md` names. Move `at most` in `reviewer.md` to 59, with this reason, or fold seven copies; until one of those happens this row is answered here. |

`unanswered` until somebody writes `accepted` or `rejected`. **After 3 days
an unanswered row fails `npm test`** — the queue can fail, so a correct report
cannot be quietly ignored the way six nights of them were (#197).

---

Read `docs/reviews/README.md` before the next run: a finding that keeps
reappearing is a finding that needs a guard, not a third mention.
