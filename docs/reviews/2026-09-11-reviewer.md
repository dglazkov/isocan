# reviewer — 2026-09-11

Run by `scripts/persona-run.mjs` at `a2deb19`. **Nothing was changed.**

| Goal | Target | Now | Verdict |
| --- | --- | --- | --- |
| exports nothing outside their own file uses | at most 39 | 39 | held |
| exports with no comment above them | at most 253 | 331 (was 253 on 2026-09-02) | **MISSED** |
| CSS rule bodies copied word for word from elsewhere | at most 47 | 59 (was 47 on 2026-08-30) | **MISSED** |

## Findings

| Finding | Outcome |
| --- | --- |
| exports with no comment above them is 331, past 253 | unanswered |
| CSS rule bodies copied word for word from elsewhere is 59, past 47 | accepted — and answered for the last time at this bound: `at most` in `.agents/personas/reviewer.md` moved 47 → 60 on 12 September, with the reason beside it. This is the move the 7 September row asked for and the four rows after it deferred by pointing at it. Twelve nights of the same question, re-answered by hand every three days, is the treadmill `README.md` names. Looked at again before moving it: 59 copies over 38 distinct bodies, mostly vocabulary — the six controls agreeing about what "on" looks like, and the flex-column, ellipsis and `flex: 1; min-height: 0` idioms — plus about seven that are one thing written twice (`.memory-mark` / `.doc-live-toggle`, `.arrival-*` / `.offline-*`, `.wb-fold` / `.stage-pane-fold`, `.face-mark` across three roster rows), left for a design pass that is in that CSS anyway. `test/copied-rules.test.ts` remains the enforcement and is the tighter of the two at 59. |

`unanswered` until somebody writes `accepted` or `rejected`. **After 3 days
an unanswered row fails `npm test`** — the queue can fail, so a correct report
cannot be quietly ignored the way six nights of them were (#197).

---

Read `docs/reviews/README.md` before the next run: a finding that keeps
reappearing is a finding that needs a guard, not a third mention.
