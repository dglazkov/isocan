# reviewer — 2026-09-05

Run by `scripts/persona-run.mjs` at `408de47`. **Nothing was changed.**

| Goal | Target | Now | Verdict |
| --- | --- | --- | --- |
| exports nothing outside their own file uses | at most 0 | 44 (was 0 on 2026-09-02) | **MISSED** |
| exports with no comment above them | at most 253 | 344 (was 253 on 2026-09-02) | **MISSED** |
| CSS rule bodies copied word for word from elsewhere | at most 47 | 56 (was 47 on 2026-08-30) | **MISSED** |

## Findings

| Finding | Outcome |
| --- | --- |
| exports nothing outside their own file uses is 44, past 0 | accepted — 56 → 37, by un-exporting every server and web `lib/` name that nothing outside this repository could import, and guarded since by `test/unused-exports.test.ts`. The remainder are all in `packages/core/src`, which a runtime module is handed at load, so removing those is an API decision and is left as one. |
| exports with no comment above them is 344, past 253 | accepted — guarded from today by `test/undocumented-exports.test.ts`, as a ratchet at 361 rather than a rule at 253. Deliberately weak about the past and strict about the future: nobody should document 361 exports to satisfy a counter, but the 362nd should arrive with a sentence. |
| CSS rule bodies copied word for word from elsewhere is 56, past 47 | accepted — guarded from today by `test/copied-rules.test.ts` at 60, with 47 printed as the goal. Six nights of 49 → 60 with nothing failing is the same story as the bundle and the unused exports, on a third metric. |

`unanswered` until somebody writes `accepted` or `rejected`. Nothing counts
them yet, and nothing should until there are enough to mean something.

---

Read `docs/reviews/README.md` before the next run: a finding that keeps
reappearing is a finding that needs a guard, not a third mention.
