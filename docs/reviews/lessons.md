# Lessons

Failure modes this codebase has actually produced, and what now catches each
one. Not a style guide and not a wish list — every entry happened, and the
"guard" column is the thing that would have caught it.

The point is the pattern, not the incident. A bug that can only happen once is
not a lesson; a bug whose *shape* recurs is, and three of these have already
recurred.

## The guards that exist

| # | The shape of it | What it cost | Guard |
| --- | --- | --- | --- |
| 1 | **A search-and-replace drops a selector's scope.** Replacing `X {` matches `X` in its scoped forms too, leaving the tail behind as a bare rule that now applies to everything. | Twice. First mangled `.project-editor`; then `.face.away .face-mark` lost its scope and dimmed *every* face, so a canvas full of live people rendered as a canvas where nobody came (`0e51081` → `8cafca5`). | `packages/web/test/oneblock.test.ts` — one canonical block per class, and nothing dims a `.face-mark` outside `.away`. |
| 2 | **A nullable interpolated into a template string becomes the word "null".** | `PresenceSession.label` is a display override, absent unless somebody passes `--label`, so the canvas said **"null is working"** over an item — for every agent that ever ran `isocan session start`. | `packages/web/test/presence.test.ts` — the working-here name falls back through the registry, and never renders "null". |
| 3 | **An HTML presentational hint wins when CSS only sets the other axis.** `width`/`height` attributes have a *used* value; setting `width` in CSS leaves the attribute's height in force. | The marketing hero shipped 1158×1346 on a 2880×1346 file — a stretched screenshot, on a page selling a design tool. | `packages/web/test/shot.test.ts` — every image gets `height: auto` (a blanket `img` rule in `styles.css`), and no rule pins an image's height. Moved with the image when `marketing/` was folded into the app's front page and the directory deleted; the guard came across because the class of bug did, and its declared-size case now reads the file's own header from `lib/shot.ts`'s single home for those four facts. |
| 4 | **A test written around the keystroke instead of the invariant.** | The first version of #3's guard required any rule setting an image's *width* to also set `height: auto` — so sizing with `max-width` reproduced the identical squash with the suite green. | Mutation testing. Break it several ways before believing a new test; if only your own typo is caught, the test is an anecdote. |
| 5 | **A guard that restates the rule instead of importing it can only test itself.** A test that defines the logic locally and then asserts on it is green whatever the app does. | #2's guard. `presence.test.ts` defined its own `nameOf` and made four confident assertions about it, importing nothing; the rule it named lived inline in a zustand selector in `ItemView.tsx`. Reintroducing the exact "null is working" bug left it 10/10 green. The local copy had also silently drifted from `actorNameIn`. So lesson #2's guard was decorative for its entire life. | Give the rule ONE home and point the test at that home: `sessionName` in `packages/web/src/lib/names.ts`, imported by `ItemView.tsx` and by the test. If a guard imports nothing from `src/`, it is not a guard. |
| 6 | **A mutation that HANGS is not a mutation that failed.** A loop whose only exit is a helper's side effect turns a broken helper into a wedge, not a red test. | Making `UndoStacks.discardUndoTarget` a no-op wedges `Engine.undo`'s `for (;;)` inside the single-writer chain: the daemon stops answering, the run never ends, and 15 minutes later there is nothing to read. In production that is a canvas that stops responding to everybody, from one actor pressing ⌘Z. | `packages/server/test/undo-stacks.test.ts` asserts the contract directly — discarding never hands the same seq back twice — so the no-op fails in 3ms with a reason. When a mutation makes the suite hang rather than fail, that IS the finding: the invariant needs a test one layer below the loop. |
| 7a | **A walk whose answer for one item depends on another item's PENDING answer is decided by iteration order.** Reading a neighbour's current state instead of its outcome makes the result a function of the list, not of the data. | Phase 9's provenance sweep. Jordan's daemon was admitted by a pass from her tab; considering the daemon first resolved its chain through a tab whose grant had just been revoked, so the daemon was expelled a moment before the tab re-rooted onto the grant that had invited her by name. Two badges of one person, one kept and one thrown out, by array order. | Decide each item once, recursively, and let a dependant adopt its dependency's OUTCOME (`server/sweep.ts`'s memoized `decide`). The guard is `sweep.test.ts`, whose cases are the design's named failure modes — a chain, a chain with a dead middle, a cycle — rather than the happy path. Write the test from the failure the design warns about and it fails on the first run. |
| 7 | **Anything that edits source on disk will be committed by somebody else.** Concurrent agents (or a stray `git commit -a`) capture whatever the working tree holds at that instant. | A mutation was live for ~20 seconds while a scoped test ran, and `110027d` — a commit about persona frontmatter — swept up the reducer line with it, removing `item.add`'s trash check and leaving `main` red. | Run mutation testing (and any deliberate temporary break) in a `git worktree` with `node_modules` symlinked, never in the working checkout. Check `git log -- <file>` for the file you were mutating before you believe a clean `git status`. |
| 8 | **A measurement that reports zero when it is broken.** `Runtime.evaluate` answers a malformed expression with `exceptionDetails` rather than rejecting; a helper that reads `result.value` and ignores that returns `undefined`, and every `?? []` downstream turns a broken instrument into a clean bill of health. | `scripts/grade.mjs` shipped its first version scoring a page with a stretched image at 8/8. Every rendered check had been vacuous from the start. | `--selftest` against `test/fixtures/deliberately-bad.html`: seven checks, all of which must fire on a page built to break them. |
| 9 | **A symmetric predicate tested on only one of its axes.** An overlap, a distance, a bounds check — the code says x and y and the fixtures only ever say x. | `placement.ts`'s `overlaps()` could have its ENTIRE y test replaced with `a.y === b.y` — a box one pixel lower counts as clear — with the core suite green, 340/340. The x mutation died instantly. The cause is that the tie-break sends displaced items right, so every fixture in `placement.test.ts` laid itself out as a row and the vertical half was never exercised. It matters because `item.add` resolves through this inside the reducer, so a half-blind overlap test is a canvas that replays differently. | `placement.test.ts`'s `describe("overlap is symmetric")` — the same collision above, below, left and right of the asked-for spot, with neither coordinate shared. Generally: when a rule names two axes, write the case as a loop over both. |
| 10 | **A rule with N copies, fixed and tested on one of them.** A `switch` with an arm per direction, a helper inlined at four call sites, a validation repeated per op. | The `⌘Up walked sideways` fix (`3edcea0`) landed the same day as this review. Applying the identical one-character regression to Down and to Left left the web suite green, 178/178 — the fix was written for Up and tested for Up, and the other three arms inherited nothing. This is lesson #4 wearing a different coat: the invariant is "leaving in a direction means clearing the edge you left", and the invariant has no direction in it. | State the case ONCE and run it over every copy. `spatialnav.test.ts`'s `describe("the same rule, on all four arms")` loops the four directions with a negative control each. When you fix a bug in one arm of a switch, grep the switch. |
| 11 | **A test that derives its fixture from the value under test moves with it.** | Caught in this review, in a test written twenty minutes earlier: `treats a gap narrower than the clearance as occupied` built its neighbour at `size + PLACEMENT_CLEARANCE - 6`, so setting the constant to 0 moved the neighbour to 6px of OVERLAP and the case passed for the wrong reason. Zero information, full confidence. | Fixtures are literals; the constant gets its own assertion (`expect(PLACEMENT_CLEARANCE).toBeGreaterThan(SIX)`). And for a tuning constant that must not be frozen, BRACKET it — one case that fails if it goes lower, one that fails if it goes higher (`spatialnav.test.ts`, 2 < w <= 3). |
| 12 | **The default test timeout runs straight through the middle of this suite's cost distribution — and it has been fixed three times, one named test at a time.** | 5 seconds, against tests that spawn real daemons and real `bin/isocan.js` processes. Measured 2026-08-24 under 24x CPU oversubscription: **90 tests over 2500ms**, slowest 14.5s, and **7 failures** — five at the 5.0-5.5s timeout signature in `identity.test.ts`, `restart.test.ts` and `session-identity.test.ts`, plus two cascades behind them. It had already been "fixed" for `daemon.test.ts`'s hundred-item move and, by CI rather than locally (`3c7825e`), for ONE case in `session-identity.test.ts` — a file where four were over the line. | `vitest.config.ts` sets `testTimeout`/`hookTimeout` to 30s with the measurement beside it. Same load, 7 failures -> 0. The general shape: **when a limit fails a test, ask what fraction of the suite is near that limit before raising it for the one that complained** — and a timeout in a file that shares state is never only one failure, because the aborted test leaves its daemon behind. |
| 13 | **A guard nobody runs.** | `scripts/grade.mjs --selftest` is a genuinely good instrument — 9 deliberate breaks, 9 caught by name, including the original silent-zero bug. It is invoked by no test, no npm script and no workflow; only by a sentence in `docs/projects/evals/plan.md` saying to run it. lessons.md #8 records it as *the guard*, which makes the guard against a silent measurement itself something somebody has to remember. | Wire it into the release workflow beside `ISOCAN_REQUIRE_EMULATOR=1`. Until then it is a habit, and the file this list is in says a habit with no enforcement is a wish. |
| 14 | **A check whose answer cannot be "no".** Not a broken check — a check that is structurally incapable of failing, and therefore reads as a standing pass. | Three separate times in one weekend: a test asserting against its own copy of the rule it guarded; a grader whose probe returned `undefined` so every reading fell to empty and a broken page scored 8/8; and a contrast badge taking the BETTER of black-on-colour and white-on-colour, whose floor across the entire sRGB cube is 4.58:1 — while a real 3.27:1 failure sat two rows away in the same document. | Ask of every new check: *what input makes this say no?* If the answer needs thinking about, search for it — `readableInk`'s floor was found by walking the colour cube, and `grade.mjs` by mutation. A check with no reachable failure is decoration, and decoration that looks like a measurement is worse than nothing. |
| 15 | **Two measuring systems meeting at a state boundary.** One piece of chrome whose SIZE is screen-measured and whose OFFSET is world-measured reads fine at 100% and drifts everywhere else — and the drift only becomes visible when some *other* state adds a correction in the second unit. | The item title row. `top: -20px` put it 3.6 screen px above the card at 100% zoom and half a pixel at 13%, because the offset was world units while ItemView counter-scales the label itself. Selecting then added a flat 7 screen px to clear the corner handles, so title and star hopped 7 up and 7 in on every click — a 2x jump at 100% and a 12x one zoomed out. The correction was correct; the baseline it corrected was the bug. | `packages/web/test/titlebar.test.ts` — the row is placed in screen pixels, and no selected-state rule sets a placement property at all. The general shape: **when chrome needs a nudge in one state, check whether the OTHER state is measured in the same unit before adding it.** Here the fix deleted the correction: a row raised 7 screen px clears handles that reach 6, in both states, so the step-in it used to need was redundant. |
| 16 | **A cache read as the truth it caches, in the one moment two processes hold it.** A detection that reads "the real value" from an in-process cache is only as fresh as whatever invalidates that cache — and if invalidation rides on this process's own writes, a process nobody writes through is stale forever and cannot tell. | #85. The heartbeat's tip was read from the engine's runtime "rather than counted along the broadcast path", and in a Cloud Run rollout the runtime WAS the broadcast path: the draining instance held the tab's socket, every write landed on the new instance, the old cache was dropped only by its own fenced append, and it never appended. The tip agreed with the frozen tab and confirmed the freeze. Two `green` advances bracketed the reported window. | `packages/server/test/rollout.test.ts` — two daemons over one store; ops through the second must make the first's next beat hang up on the room with `WS_BEHIND`, and a fresh hello to the first must come from the store. Generally: when a check is meant to catch "this process has gone stale", it must read from something the OTHER process also writes to. |
| 16 | **An existence check that any RELATIVE can satisfy.** "The thing I am about to check still exists" is written as a substring or a mention test — and a descendant rule, a variant, or the same name with a suffix answers yes on its behalf. The check then reads as a standing pass while the thing it guards is gone. | Found by mutation on 2026-08-24b, in the two files whose whole job is this. `worldchrome.test.ts`'s `has rules for every selector it claims to check` — written, in its own comment, "so the file cannot pass vacuously the day a class is renamed" — passed with `.item.peeked`'s ring **deleted outright** (`.item.peeked .item-titlebar { opacity: 1 }` kept the list non-empty, and with no lengths left the sizing sweep had nothing to reject), and passed with `.resize-handle` renamed to `.resize-handle-base` (the four `.resize-handle-nw/ne/sw/se` rules are superstrings of it). Web suite green, 226/226, with the peek ring gone and every corner handle unsized. `titlebar.test.ts`, written the same day, had the identical hole: deleting the rule that places the title row left it 3/3 green. | Two questions, two checks. **Does the rule exist under exactly this name** — a selector-list `includes`, never a substring, and a class name ends where `[-\w]` stops. **And does it still do the job it is named for** — `.item-titlebar` must carry a `top`/`bottom` that divides by `--scale`, world chrome must carry at least one `calc(… var(--scale))`. Existence and function are different claims; a test that only asks the first one has not asked anything. Proved both ways in both files: 12 mutations, 12 killed. |
| 17 | **A floor that exceeds what exists is not a floor, it is an overlap with a reason.** `Math.max(MINIMUM, available)` where MINIMUM is a wish and `available` is a fact. | `nameRoom` was `Math.max(MIN_NAME_ROOM, width * scale - STAR_ROOM)`. At 13% zoom a 480-unit item is 62 screen px and the star wants 26, so the floor handed the name 48 and it was drawn straight through the star. It shipped, and it shipped green, because the rule lived inline in a component nothing could import — lesson #5, one file along. | The rule moved to `nameRoom`/`nameFits` in `packages/web/src/lib/chrome.ts` and `chrome.test.ts` imports it. And the guard is stated **without the constants**: a floor is a BEND, so assert the function is LINEAR — `nameRoom(2w, s) - nameRoom(w, s) === w * s` at every size and zoom. That survives retuning `STAR_ROOM` and cannot be walked past by a floor of one pixel. 9 mutations, 9 killed, including a 1px floor. Generally: **when a guard would otherwise freeze a tuning constant, look for the shape the constant cannot change** — linearity, monotonicity, "the parts sum to the whole". |
| 18 | **A scan justified by "the answer is usually near the front", tested only at the front.** | `Engine.alreadyWritten` walks the oplog backwards for a replayed op's key. Truncating it to *look at the most recent entry only* left `idempotency.test.ts` green, 7/7 — every case in the file retried with nothing in between. The real shape is the opposite: a tab that lost its answer has a queue behind it, and the seconds before it retries are seconds in which anybody on the canvas can write. Past the depth the scan reaches, a replay is applied twice, and phase 10's whole argument is that it must not be. | `idempotency.test.ts` now buries the retry — five of the actor's own ops on top, and a second case where the op in between is **another actor's**. 4 mutations, 4 killed, including a clean depth-3 truncation. Generally: if a search's comment argues about where the answer *usually* is, the test has to put it where it usually is not. Same shape as an LRU, a "check the last N", or any cache with a horizon. |
| 16 | **A bad value that does not throw, and then serializes to something legal.** `NaN` compares false against everything, so nothing downstream raises — it lays out, it renders blank, and `JSON.stringify(NaN)` is `null`. The corruption is written to the log, permanently, by code that never errored. | `isocan mv <item> --to 300,200`. `mv` allows unknown options so `mv itm -80 420` can pass a negative x; the same permission made `--to` an operand, so `Number("--to")` was the x. The item's position became `"x": null` and it was unreachable from both surfaces. `undo` does not help — the op applied. | `packages/core/test/geometry.test.ts` — every op carrying a coordinate or a size refuses NaN and both infinities, in the REDUCER, because the CLI is not the only writer. The shape: **when a parse can produce a non-value, ask what that non-value serializes to.** A guard at the surface that produced it would have fixed one caller; the layer both surfaces share fixes the class. |
| 17 | **Deduplicating by a key that is finer than the thing being deduplicated.** A list keyed by A, filtered for uniqueness by B, where one A can own several Bs. | The facepile. `canvasStore` filters your own tab out of `sessions` **by session id**, which made appending your own face unconditionally look safe — but the pile is keyed by ACTOR. One person with two surfaces (an agent holding a terminal and a browser tab, or anyone with the canvas open twice) is one actor with two sessions, so the other surface survived the filter and you were drawn twice, with a React duplicate-key warning per render. React's documented answer to duplicate keys is that children may be duplicated **or omitted**. | `packages/web/test/facepile.test.ts`, against `lib/facepile.ts` — the rule was extracted so a test could import it rather than restate it (#5). The shape: **the key you render by is the identity of the row; every filter feeding that list must use the same key.** |
| 18 | **New chrome takes its room from a shared budget, so it silently moves the threshold where OTHER chrome disappears — and anything hidden as a pair disappears together.** | Adding the kind glyph to the title row did both in one commit. Its 16px came out of the name's budget, so on a 480-unit item the name stopped being drawn below 18.8% zoom instead of 15.4%; and because the glyph and the name lived in one element that was hidden as a unit, the band from 12% to 18.8% showed a bare star — a card saying nothing about itself, where a shape would have read fine. Both shipped green: the existing guard asserted the budget arithmetic, which was correct, and nothing asserted what a person could SEE. | `chrome.test.ts`'s "what a card says as it shrinks" walks 1–300% and requires the card to say something at every size the chrome is shown at all. The rule is an ORDER, not a budget: the glyph yields to the name (which is the more specific answer — "which one" beats "what kind") and then outlives it (a shape reads where text is a smudge, the same argument that keeps the star). Generally: **when you add to a row, ask what the row already drops when it runs out of space, and where your new thing sits in that order.** |
| 19 | **`var(--typo)` is not a wrong value, it is NO value.** A custom property nothing defines, used without a fallback, makes the browser drop the whole declaration — no warning, no throw, no fallback. So a one-character token name is invisible: the rule parses, the build is clean, the suite passes, and the element renders with whatever it would have had anyway. | Caught the day full screen was built. `.fullscreen` asked for `var(--bg)`, which this stylesheet has never had — the palette calls it `--ground` — so an element whose entire job was to COVER the canvas was transparent, and the canvas showed through the thing hiding it. Same shape as #16: a bad value that never threw. | `packages/web/test/tokens.test.ts` — every `var(--x)` in the stylesheet names something defined, with the four properties React sets inline listed as the deliberate exceptions rather than tolerated by a loose regex. Comments are blanked (preserving line numbers) so the rule can explain itself without tripping over its own example. |
| 20 | **`element.click()` is not a click.** It invokes the handler directly and never consults hit testing — so it passes identically on a button that is visible and pressable, on one buried under an overlay, and on one inside a `pointer-events: none` container. A test written that way says "the handler works", which was never in doubt, while the question is "can a person reach it". | The Full screen button shipped unclickable. It sat in `.item-hint`, which is `pointer-events: none` on purpose so a hint never eats a click meant for the canvas, and the button did not opt back in. It rendered, it looked pressable, and `elementFromPoint` over its middle returned the canvas viewport. The verification that missed it was mine, in a browser, on the real page — the method was wrong, not the environment. | `chrome.test.ts` asserts the pair: the strip stays inert, the button takes its events back. And the habit: **verify a control with `document.elementFromPoint(x, y)` or a real pointer at real coordinates, never with `.click()`.** Same family as #14 — a check whose answer cannot be no. |
| 20 | **The instrument answers cheerfully too.** #14 asks what input makes a check say no. Its sibling: what makes a *measurement* say the wrong thing confidently. An admin or debug API that renders a value from a different code path than the one users hit will report the old world long after the new one is live. | Phase 13.5's sender domain. `accounts:sendOobCode` with `returnOobLink:true` renders the DEFAULT sender domain whatever the custom-domain config says. It was probed twice, answered `firebaseapp.com` twice, and the conductor was one message from reporting the change ineffective — while real mail had *already* been arriving from `noreply@dev.isocan.io`. The reverse of a silent zero: a confident stale reading. | No test can guard a third party's debug endpoint. The habit: **verify a change through the path a user takes, not the path that is convenient.** For a sent email that means an inbox; for a rendered page, a browser; for a deployed build, the deployed URL. When an instrument and reality disagree, suspect the instrument before concluding the change did not work. |
| 21 | **A version break that only hits WRITES is a quiet one.** Reads keep working, so every surface looks healthy until somebody tries to change something — and the failure lands far from the cause, usually as a 500. | Phase 13.5 renamed `projectId`→`canvasId` on the wire. A pre-rename CLI listed canvases from a post-rename daemon perfectly and answered `error: internal error` on the first write. Nothing in that message names the cause, the remedy, or even that a version is involved. | `packages/server/test/stale-client.test.ts` — one `preValidation` hook and the socket upgrade answer **426** with a sentence naming the cause and the upgrade command, built from `INSTALL_SPEC` rather than typed. Two shapes to copy: detection needs **both** halves (new key absent AND old key present — mutating it to the missing half alone turned all seven tests red, because the door's own bearer request carries no `canvasId`), and a negative control asserts a request with *neither* key keeps its ORIGINAL refusal, so a malformed request never masquerades as a version problem. |
| 22 | **A positioning box claims a hit area for pixels it never paints.** The inverse of #20, and it hides better: there is no `pointer-events: none` to spot and nothing visually wrong. A wrapper exists only to place its child, it is bigger than that child, it floats above the canvas — and the difference is a rectangle of *empty* that swallows clicks a person can see straight through. | `.minimap-dock`: a 168×108 box around a 138×89 map. Folding it made this WORSE, not better — folding sets `pointer-events: none` on the panel and leaves the box, so the corner still ate clicks with nothing drawn there at all. It cost nothing for months because nothing else wanted that corner; then item reaction chips arrived under the item, and flying to an item centres it, so **every** "go to item" from the marks dock parked the new chips underneath and they could not be clicked. The same shape is already in the changelog three times (identity menu under the trash panel, beacon card under the right dock, minimap under a left panel) — each time fixed as one incident. | `layers.test.ts`'s `floating chrome does not eat clicks it cannot see`: the dock yields, the map and the handle claim back, and the folded panel still gives its own back. The rule to carry: **a container that paints nothing takes `pointer-events: none`, and whatever is DRAWN inside it takes `auto`.** Yielding alone is #20's bug; claiming back alone is this one. And it was found by #20's habit — `elementFromPoint` over the chip's centre — not by looking at it, because it looks fine. |
| 23 | **`void somePromise()` starts a writer that `close()` does not know about.** Fire-and-forget is a statement about the RESULT — nobody needs the number it returns — and it gets read as a statement about the LIFETIME. The task keeps writing after the thing that started it has said it is shut, and the failure surfaces wherever the files happen to land, which is never the file at fault. | Third time in `daemon.ts`, and the comment on `close()` already narrated the first two: the sockets, then the desk writes `engine.settled()` exists to catch (`ENOTEMPTY … rmdir …/desk`). The third was `void`-ing the boot-time `backfillLastOp`, which walks the home rewriting `project.json` through `writeFileAtomic` — so a closed daemon went on dropping `.tmp-*` files into `projects/<id>/` for milliseconds afterwards. It presented as **`replica.test.ts` failing ~60% of runs** with `ENOTEMPTY … rmdir …/projects/prj_acme` from its own `afterEach`, in two tests that were the only ones booting a SECOND daemon over a home that already held canvases — the one arrangement giving the repair anything to do. Nothing in that message names a background task, so it reads as a teardown bug in a file about replicas. | `packages/server/test/shutdown.test.ts` — written against the SHAPE, not the backfill: does anything under the home change after `close()` resolves. Two halves, because a shutdown owes both — a flag that cuts the walk short at a canvas boundary (so a large home does not delay exit) and a promise `close()` awaits (so the canvas in flight finishes first), which is the arrangement `startGcSweeper` already had. Note the third case: dropping only the `await` and keeping the flag passes the filesystem check **every time**, because `close()` does enough other work afterwards that one more canvas usually lands inside it — so that half is asserted by spying on the write and slowing it past the rest of teardown, turning a probable race into a certain one. 4 mutations, 4 killed. Generally: **`void` on a promise that writes is a shutdown bug; give it a handle and a stop flag at the moment you write it.** |
| 24 | **A field-picking deserializer keeps the write and drops the read.** A backing that rebuilds rows property-by-property (`toGrant`, `toRecord`) accepts a new field on `set()` — the writer spreads the whole object — and then reconstructs the row without it on every `get()`. The field is IN the database and invisible to the program, so the write path, the response, and every test on a whole-object backing all say it worked. | #88's `capability` on grants, on prod, the same day it shipped. `CloudDesk.putGrant` stored `view` via `jsonSafe(grant)`; `toGrant` rebuilt rows from six named fields and dropped it. "Can view" answered 200 with the right row in the response, the dialog re-read `edit`, and the door went on admitting editors — while `FileDesk` (which stores rows whole) passed the entire suite and the real-browser walk. Found by the user, on isocan.io, on an existing canvas. | `desk-conformance.ts` asserts the new field ROUND-TRIPS through `putGrant`/`grantsFor` — and it runs against BOTH backings, which is the whole point of a conformance suite: the file desk cannot vouch for the cloud desk's deserializer. The habit: **when a type grows a field, grep the backings for functions that rebuild that type field-by-field**; every one of them is a place the field silently dies, and the conformance case is written before the field is trusted. |
| 25 | **A derived field on a shared type that only one side of the wire ever populates.** The type says the fact is there; the transport never carries it; every reader across the boundary silently gets `undefined` and the `?? false` downstream turns "we never sent this" into "it never happened". | `LogEntry.undoneBy` is reconstructed in the server's in-memory `UndoStacks` and deliberately never written to the log file — so `isocan evals corpus`, built on it, reported **0 ops undone across every canvas at this home**, on a home where `plan.md` had hand-counted 16. A report whose entire job is to be believed, confidently wrong in the safe-looking direction. Lesson #14's shape arriving through the type system rather than through a check. | `undoneSeqs` in `packages/core/src/undone.ts` derives it from the log's own `cause` entries, which DO travel, and `chooseRetained` in `gc.ts` now shares that fold instead of keeping its own copy. `packages/core/test/undone.test.ts` covers undo, redo, and undone-redone-undone. The general shape: **before reading a field across a boundary, find the line that writes it on the other side.** If it is only ever assigned in memory, it is not part of the contract, and a comment saying "derived" is the tell.
| 26 | **A join over "any later X by the same person in the same thread" is a join over the whole channel when the thread is the Chat.** The predicate reads as local to one ask; in a shared thread it reaches every earlier ask that person made. | `buildCorpus` scored a `/cancel` against every earlier ask by its author in the thread. One `/cancel` in the Chat marked sixteen asks cancelled, fourteen of them answered hours before, and the 1 Sep headline carried "31 cancelled" as a finding. Found by reading the rows (the Stage 1 labelling, 3 Sep), not by a test — the number looked plausible. The same reading found the "one ask in four silent" headline was agents' receipts nobody replied to. | `packages/core/test/evals.test.ts` — one `/cancel` calls off the ask it follows and leaves the answered ones before it answered; the cancel belongs to the asker's most recent ask. The general shape: **a later-in-thread join needs the nearest own comment, not any own comment, and a caveat on a number ("upper bound") is not a correction to it** — the caveat was written and the number was believed anyway. |
| 27 | **A grader that knows three of the four ways a thing can be right fails the fourth as wrong.** `grade.mjs` named a control by its text, `aria-label`, `title` or `aria-labelledby` — and not by its `<label>`, which is how most fields and every wrapped checkbox are named. | The golden suite's own checklist answer, eight checkboxes each wrapped in a label, scored eight nameless controls. Caught the day it was written, by the suite's selftest requiring its reference answers to pass — the first thing the new instrument measured was the old one. Left alone it would have failed every correctly built form on every canvas it graded, and false failures are how a grader becomes decoration (the inline target-size exception was the same shape). | `test/graders.test.ts` and `grade.mjs --selftest` still hold; `golden.mjs --selftest` now requires the checklist answer's labelled controls to pass "every control named". The general shape: **a check that enumerates the accepted forms of correctness must enumerate all of them, and a reference answer built the ordinary way is the cheapest test of whether it does.** |

## The habits, from bugs with no test to give

- **Verify against what is actually served.** The daemon serves the built
  `packages/web/dist`, not Vite. A fix "not working" was a fix never built —
  `npm run build` after every web change, or you are testing yesterday.
- **A zustand selector returning a new array, object or Set every call is an
  infinite render loop.** It has happened three times, once blanking the whole
  app. Return a scalar and derive outside, or select the pieces.
- **Measure→setState in a layout effect with no deps loops too**, even when the
  value stabilises, because StrictMode double-invokes. Write placement straight
  to the DOM instead.
- **Guard keyup wherever you guarded keydown.** Typing "p" in a text field
  selected the Pen because only the keydown checked. Guard on the press you
  started (`penDownAt.current !== 0`), not on the event target — a target check
  strands a real hold when focus moves mid-press.
- **A stamped name is a log entry, not an identity.** Mentions matched the name
  frozen on the comment, so renaming "Dion 2" to "Di" meant `@Di` reached
  nobody. Resolve against the current registry (`actorsAnswerTo`).
- **Hang a feature off a signal the path actually touches.** Cancellation was
  hung off presence updates, but `ls` never touches presence, so it never
  arrived. Ask what the command already does before choosing where to listen.
- **A code comment that reasons about browser behaviour is a hypothesis until
  somebody measures it.** `isOpen` held the blob route open for four phases on
  a correct fact about sandboxed iframes (opaque origin ⇒ null
  site-for-cookies) applied to the wrong request: what has no cookie is what
  the loaded document requests AFTERWARDS, while the iframe's own load is
  issued by the parent, same-site, and carries the `Lax` cookie. Ten minutes
  with a Node server that logs request headers and a real Chrome settled it.
  Cheapest instrument in the box, and the one this repo keeps not reaching for.
- **When a behaviour inverts, the copy describing the old one is a bug in
  every place it survives.** `isocan share --link off` printed the new "1
  expelled" and then, two lines down, the old "people already on this canvas
  keep their access". Both lines were individually defensible and no test
  compared them, because no test reads a command's whole output as prose. The
  places are found by running the thing, not by grepping for the feature's
  name.
- **Assert your probe's preconditions.** More browser-probe "failures" in this
  repo have been the probe than the app: `elementsFromPoint` cannot see
  `pointer-events: none`, the route is `/p/` not `/canvas/`, a tap of `P`
  *toggles* the Pen off, and a stale log window shows a run that already ended.
- **Python's `str.replace()` replaces every occurrence.** Always assert the
  count before writing the file. Two of the worst entries above start here.
- **A guard that counts duplicates only sees the second one.** `oneblock`'s
  "each class once" fires when a dropped scope leaves a *second* bare rule; for
  the 57 classes in `styles.css` that have never had a bare rule, the leftover
  is the first and the count is 1. Ask what the guard's shape misses before
  believing the class of bug is closed — and prefer an invariant derived from
  the file (a chained class is a modifier, and modifiers are never bare) to a
  list somebody has to maintain.
- **A scale test's timeout is set from what it costs, not left at the default.**
  A 200-item test at 4.3s alone passed alone and failed 3 of 3 full runs on
  vitest's 5s default, because a full run is fifteen files fighting for the
  same cores. Repeat a suite before calling it green: a new test that only ever
  ran alone has not been run.
- **A guard clause nothing reaches is the author's belief, not the system's
  behaviour.** `parseRange` has four of them — `start >= size`, `end < start`,
  the suffix clamp `Math.max(0, size - wanted)`, and `bytes=-0` — and every one
  could be deleted with `blobs.test.ts` green, because the file only ever asked
  for ranges comfortably inside the blob or comfortably outside it. Three of
  the four are what a resumed download and a media element send unprompted
  (`curl -C -` sends `bytes=<size>-` when it is already finished). When a
  function writes a branch for a boundary, the suite needs an input ON the
  boundary; otherwise the branch is documentation.
- **Do not assert the checkout's directory name.** `restart.test.ts` required
  `status().root` to match `/isocan$/`, which is a claim about what somebody
  called the folder — red in a `git worktree`, in a clone called
  `isocan-fork`, and in the tarball CI unpacks. Compare against the resolved
  repo root instead: stronger, portable, and it says the thing the test is
  actually about (the daemon runs from the tree the test runs from).
- **A habit written down is a habit written down in one place.** The evidence
  rule below was learned in `daemon-takeover.test.ts` and never reached
  `restart.test.ts`, which kept `stdio: "ignore"` and a bare
  `the other copy never came up` — and duly failed one full run on 2026-08-24
  with nothing to say about why. That is lesson #10 (a rule with N copies)
  applied to test helpers, and helpers are where it hides best, because nobody
  greps a fixture. When a habit gets a fix, grep for the other copies of the
  pattern, not for the copies of the bug.
- **A timeout message with no evidence teaches people to re-run.** `timed out
  waiting for the squatting daemon` is the same sentence for a loaded machine,
  a stolen port and a process that crashed at startup — and `stdio: "ignore"`
  had thrown away the one thing that could tell them apart. Say how long you
  waited, what the last probe saw, and whether the process is still alive; fail
  immediately once it has exited.

- **A number with no subject attaches itself to the nearest sentence.** The
  Share dialog held one `swept` count for two different revocations, so
  un-inviting one person by email rendered *"anyone with the link — 2 surfaces
  lost this canvas"* under the link toggle. Every word of it was true and the
  sentence was a lie. Report WHICH gesture a count belongs to, or put it beside
  the control that produced it — and note that the same file had already
  learned this once, in the CLI, one stage earlier.
- **A destructive gesture can remove the person performing it, and then the
  re-read lies.** Un-inviting somebody expels the caller when the caller was
  admitted by the row they just revoked — the ordinary case for anyone tidying
  up an invitation they were themselves on. The follow-up list request then
  403s, and a dialog that only trusts the re-read leaves the revoked row on
  screen with a live-looking button. Drop what you know is gone locally BEFORE
  re-reading, and translate the refusal into what actually happened. Neither
  half is reachable from a unit test: both are about the screen after a request
  that succeeded.
- **When a capability looks like it must be compiled in, check whether what
  really varies is an input the code already needs.** `attest.ts` shipped its
  roster of attesters as a constant, on the reasonable-sounding ground that
  configuration could make a home claim an attester it does not have. But the
  verification ships in every build identically; what varies is whether there
  is a project to check tokens against — and that project id is what `iss` and
  `aud` are bound to anyway. So the configuration is not a claim that could be
  false, it is the thing the work is done with, and a wrong one fails loudly at
  the first use. The constant would have made "one image, many homes"
  impossible for the sake of a lie that cannot be told.

## Working alongside agents

- **`git add -A` commits whatever the other agents have on disk right now.**
  Four reviewers ran concurrently in this checkout; one of them had a deliberate
  mutation live in `packages/core/src/reducer.ts` for about twenty seconds while
  it checked that a test caught it. A commit meant to touch four frontmatter
  blocks swept the mutation up with it, and `main` carried a reducer with
  `item.add`'s trash check deleted — a commit that failed its own suite. It was
  caught by the agent that made it, and it was never pushed.

  While background work is running, stage the paths you actually changed. `-A`
  is a claim that everything in the tree is yours, and with agents in the room
  it is not.

  **Written down and then done again the next day**: a commit about placement
  swept in a review agent's `dimmed.test.ts`. The test was good and it is
  green, but it landed under somebody else's message with somebody else's
  reasoning. A lesson nobody obeys is a lesson that needs a smaller ask — so
  the ask is now literal: `git add <paths>`, never `-A`, while a task
  notification is still outstanding.

## Adding to this file

A new lesson needs three things: the shape stated so it applies beyond the
incident, what it actually cost, and the guard — a test if one is possible, a
habit if it genuinely is not. "Be careful about X" with no guard is not a
lesson, it is a wish.
