# 19 September 2026 — The first minute's next two phases open as proposals

Most of what the history dates to this day was made on the evening of 18
September, Pacific time, and is already written up on
[that day's page](2026-09-18.md): the first-minute project (#332) and its
phases 0 to 3, and the room's `RoomTurn` fields and `RoomHold` (#333).
Across those phases, in phase 0's sandbox, install went from 14.2 s to
4.1 s, 227 packages to 3, `isocan --version` from 1.12 s to 0.10 s, and
4397 file opens to 49 — journey 1's targets met. What that page does not
say is below.

**Phases 4 and 5 each open with a proposal and stop**, as the walk says
they should, because each carries a decision that is Dimitri's.
[`guide-cut.md`](../projects/first-minute/guide-cut.md) proposes a core
guide of about 6,600 tokens against today's 46,700, names its 46 topics,
and puts three judgements to him — the largest being that most of
"Practices that earn trust" is the design stack, a manual rather than a
protocol.
[`no-secret-identity.md`](../projects/first-minute/no-secret-identity.md)
names the three shapes an agent's identity could take without holding a
secret and recommends environment variables, because #332's sandbox is
launched rather than operated and its disk is gone tomorrow. The proxy
half is separated out because it has no decision in it.

**The module guides needed no laziness.** Phase 3 listed them, but phase 1
had already made them build-time text constants, the same end by another
means. `HOME_HEADER` left the server's index with `startDaemon`: nothing
outside `http.ts` had ever read it.

**Two fixtures were wrong in ways the bundle exposed.** `setup-npx.test.ts`
assembled an npx copy with no root manifest, which npm never does and
`packageRoot()` needs. And `identity.test.ts`'s afterEach (#304) removed
its scratch directories with no `maxRetries`, which
`test/teardown.test.ts` has always refused; `main` had been red on it
since that landed.

Grades and a persona run landed as usual.
