# Changelog

One page per day, oldest at the bottom. Each says what changed and — more
usefully — *why*, because the reasoning is the part that is expensive to
recover and the diff is the part that is not.

Written from the commit history, which in this repo carries the argument as
well as the change — by hand at the end of a session, or by the nightly
workflow for the days that ended without one. Where a day's work was later reverted, the entry says so
rather than quietly dropping it: a road not taken is worth as much as the one
taken, and costs less to read.

**[The whole changelog, laid out properly →](https://claude.ai/code/artifact/0d2b285f-f913-4987-b1d9-0dbe48724106)**

| Day | | What happened |
| --- | --- | --- |
| **[28 Aug](2026-08-28.md)** | The refresh | A morning of research that mostly said no, an afternoon rebuilding the chrome, and a CLI that learns it is behind. Five research pieces: mind maps turn on the fact that **this canvas has no edges**, get answered as a file, and then get revised the same day — the mistake was in the SIZING, not the reasoning, because the nodes already exist as items and `lineage`'s `parent` already has an edge's shape, and direct manipulation settles it structurally since a dragged node's position must be a canvas fact. External memory services get "index, never record", because the memory here IS the artifact, can be UNDONE, and is SHARED by construction — the three properties adopting a store would spend. Six of seven component libraries ship React and an isocan screen is one self-contained HTML file, so there is nowhere to put them; what transfers is tokens. Motion gets sorted into three kinds and only the third — motion that DECIDES state — is missing. And the one that said yes: grouping was already implemented ad hoc three times, once per gesture, each costing a permanent plural op. `LogEntry.group` ships that afternoon as a LABEL and not a transaction, and meets its fourth caller an hour later when dropped files stop scattering. Copy/paste lands on both surfaces with the app's own clipboard, because `navigator.clipboard` is permissioned and a refusal is indistinguishable from having none — then ⌘C did nothing, exactly, reading a canvas from a closure that had not loaded. The flaky tests turn out to be pointing at a real defect: `isocan setup` could silently not redeem a pass and **exit 0**, so somebody pastes a line from a chat window, is told it worked, and is not on the canvas. Then the UI refresh in six phases: the rail floats, opening it PANS rather than reflows (docking takes width, floating borrows a pan and returns it), the shut rail becomes a 48px strip carrying unread and who is working, the lane draws what a message MADE with the rule in core rather than in a panel, the tether refuses to draw in four cases because a wrong line costs trust in the right ones, the header dissolves into three floating clusters, four insets become one `--edge`, eight buttons become one handle with a test that nothing was lost, the agent tray gives `isocan who` a home on the canvas, and full screen learns to rest — the chrome fades after a few still seconds, and a key press deliberately does not wake it, because flipping slides IS presenting. The Pen tool whited out the app — a call site that never moved while what the call WAS changed from a function into a hook — which finally buys the repo a linter, on the grounds that a conditional hook has no seam to test. And the day's real lesson, said out loud in four commits: four guards passed against the bug they were meant to catch, three of them written the same day, each found by mutation-testing rather than by being green. Auto-upgrade phase 2 lands in its own lane — a CLI that disagrees with its home says so once per sha pair, and produces NO verdict rather than a cheerful one when either side cannot say which build it is. Its review found two more bugs and a fifth guard that did not bite. |
| **[27 Aug](2026-08-27.md)** | The picker | Browsing for the directory, and two words pointing at the wrong thing. Paste-a-path only helped somebody who already had the path in their head, so the Attach row grew a `…`: one level of directories at a time from `$HOME` down, picking FILLS the field rather than binding, so the browsed route and the typed route end in exactly the same place. It is deliberately thin because it is deliberately wide — `pickList` is the first enumeration surface this daemon has ever had, listing directories nobody has bound to anything, so it names directories only, one level, never files, never bytes, never a recursive walk that would turn a picker into a map of a disk. Shaken with the tree's own discipline, `$HOME` as a ceiling with `up: null` at the top so the app hides the step rather than offering one that would be refused. The live run caught the `direction: rtl` clipping trick reordering neutral characters at a string's edges (`/Users/dalmaer` rendering as `Users/dalmaer/`). And two words were pointing at the wrong thing: staging an item did not select it, so whether the Chat attached what you were looking at depended on how you arrived — same screen, two behaviours — and the composer's placeholder said "Message the canvas", which reads as addressing a surface when you are addressing the people and agents on it. |
| **[26 Aug](2026-08-26.md)** | Trajectories | The history becomes readable, then legible. The Headlong survey's ten-minute finding — `gc` archives every compacted op into a file nothing in the product can open — gets its fix: `readArchivedLog` on both storage backings (the conformance suite now forces any backing to ship the read with the write), a route, and `isocan tail --archived` for the full history in one seq order. On top of it, `isocan recap`: the whole past at decaying resolution — recent ops verbatim, older stretches summarized into windows that double in size with each step back, every window carrying the seq span that replays it at full detail. The tiering is pure math in core; the CLI is just its first renderer. And around it, the largest day of surface work yet. The workbench lands in three versions: the agent room (zero new operations and zero new protocol messages, held there by a test), the working surface (⌘S is `item.addVersion` — the identical op `isocan edit` sends, so a web save stacks, S-fans, undoes per-actor and wakes an agent parked on `wait`), and the disk (an owner-scoped, loopback-only file tree that is deliberately NOT the admissions door, because every canvas is born with a link grant and a tree behind that door hands anyone with the link the owner's `.env`). The content origin gets a four-stage walk written under one constraint — every stage ends stable on both shapes — and takes two of them the same day, so a local item finally gets storage that survives a reload and nothing had to know about isocan for it to work. WYSIWYG V0 edits one text node in place and lands it as an ordinary version, with the unique-match refusal firing honestly on its first real outing. The canvas gets a **Chat**, and everything else becomes a **comment** — the wire untouched, since renaming `thread.setMain` would break every installed CLI; what had to agree is what a person is told. A directory can be attached from the app, typed and never picked, because the File System Access API is designed never to tell you a path — so the browser asks and the daemon does. And the day's best small bug: a drop overlay cleared by `dragleave`, which fires on whichever child the pointer was last over, so it stood over a drag nobody was making until you reloaded. Liveness by heartbeat beats liveness by ledger whenever the ledger's events are not promised. |
| **[25 Aug](2026-08-25.md)** | Phase 14 | isocan.io. The launch train's last car: `isocan-io-prod` stands up — its own project, its own Firestore, one always-on instance, a load balancer at the apex, nightly exports, an attester, and a deploy that happens only when somebody moves a tag. The CLI stops asking a stranger where their canvases should live; a machine that has never held one is pointed at the hosted home, once, with a receipt and a one-command way back — and the care is all in *who* gets flipped, since putting the default one function lower would have re-pointed every existing daemon on the day it shipped. 13.7's ingress hole was measured before it was closed: with the honest bucket already refusing at 429, eight invented `X-Forwarded-For` values each minted a badge. Going to read the log line that proves such things is what turned up the day's real find — `Fastify({})` with no `logger` key hands back `abstract-logging`, whose `warn` is `function noop () {}`, so every deliberate log line the server had ever written went nowhere, including the one 13.7 added as the way somebody at 3am sees the whole internet land in one bucket. An instrument can be cheerful too. And in the other lane: the favourites star died and reactions replaced it — marks with names on them, a canvas grouped by whatever a team decides 👀 means, a searchable picker, one under-item chrome row — with a same-day word→glyph→both reversal on the full-screen button recorded honestly. Then the evening, which was all about copies that could not say what they were. A canvas whose faces never reached its home gets observability and a repair: presence rides ONLY the per-canvas socket while writes go over HTTP, so a roster that never leaves the machine is indistinguishable from a canvas nobody else is on — and the sharpest find was an open socket counted as a success, the home accepting the upgrade and closing 4404 twenty times a second while the failure count read perfectly healthy. Presence was also the one edge-triggered thing in a level-triggered system, so a single refused relay was permanent; it is two seconds now. A build stamp so `isocan status` stops answering `0.1.0` for every copy ever shipped — with the fourth shape of `.git` found within hours, reftable, where HEAD is a stub reading `refs/heads/.invalid` and the honest answer is silence. `docs/projects/<name>/` so six bodies of work stop sharing one `design/` drawer, 131 references rewritten and 0 links broken. And a review of the day's six concurrent UI commits, whose one disease was numbers that had grown a second home: the tool rail was cleared twice, seventeen minutes apart, each commit's changelog claiming its own 76 owned it. |
| **[24 Aug](2026-08-24.md)** | Phases 9–13.7 | Identity you can prove, revocation that means something — and the home turns out to belong to the canvas. Revoking a grant now expels the people it let in — down a chain of passes — while keeping the ones another grant still covers; the order bug that decided it by list position is the design's own named failure, caught on the first test run. Identity Platform arrives as the borrowed bench: prove an inbox and a second browser becomes the person your laptop already is, with no account, no password, and no SDK parking a second identity beside the badge. Then phase 10: a tab survives its home — service worker shell, durable IndexedDB replica, writes queued and flushed before the tail. The brief's premise was measured and found false, which improved the fix. Plus: the blob route closes on a measurement that showed its founding argument was about the wrong request. Then phase 10.3 — one daemon, many homes: the home stops being a property of the daemon and becomes one of the canvas, which is what makes launch's default address safe to flip. Its sweep line turns out to have been a data-loss bug waiting for a second home, and the design's "transient and self-healing" name flap is measured, found permanent, and found to predate the phase. Then 10.5 — two doors into the repo — whose real find was that writing the doc uncovered a 10.3 bug on the very path it protects: the first `isocan home` on a pre-`homes.json` rig froze every local canvas at that home, under a verb printing "nothing already here moved". Three places disagreed about one invariant, and the first three regression tests passed against the bug. Then 13.5 — the front door: the origin stops meeting strangers with "pick your name" and wears Scene 0's three steps instead, `marketing/` folds into the page that is actually served, and the rename finally makes the code say canvas. Its detour was the best part: the magic-link spam fix turned out to be code as well as DNS, because the provider moves the From: address and the action-link domain together — so the daemon serves the sign-in handler itself rather than sending people to a second origin, and the link now arrives in an inbox from `noreply@dev.isocan.io`. Plus a lying instrument that reported the old sender domain twice while real mail already carried the new one, and a wire break that only hit writes. Then 13.7 — the innkeeper's obligations: the home collects its own garbage on a timer, the door is metered, and `/terms` says plainly that the operator can read your canvas. Its best catch was a mechanism that could not fire and looked exactly like one with nothing to do — a GC timer whose first tick was an hour away in a process that scale-to-zero reaps after fifteen minutes, silent because an idle sweep logs nothing by design. Its sibling: a rate limit keyed on the socket address puts the whole internet in one bucket behind a load balancer and refuses every visitor at 429, looking precisely like a rate limit that works. |
| **[23 Aug](2026-08-23.md)** | 12 commits | The lid closes, and the daemon learns to dial. Phase 6: a local daemon stops being a home and becomes a replica of one — it dials, forwards every write, and reconnects by saying "I have through N". Verified against dev.isocan.io over wss:, which also caught yesterday's health-path finding expiring inside a day. Then phase 7: the link grant, a door that refuses, and the day's real lesson — the same bug three times, because this system's default answer to a wrong address is a cheerful one. The best catch was a guard that said yes to the empty list: an emptied live log answered a resume with "you're current" while four ops were missing, and a quiet canvas leaves nothing to correct it. |
| **[23 Aug](2026-08-23.md)** | 39 commits | Phase 6 to phase 9 in one Sunday — the daemon learns to dial, a link becomes a share, escalation becomes one command, and identity becomes something you can prove *and revoke*. Alongside it: a design system drawn as itself rather than as text, items that stop landing on each other, ⇧F to fit an item to its content, an eval plan, and four standing reviewers with the memory that makes them cumulative. |
| **[22 Aug](2026-08-22.md)** | 46 commits | The home goes up, and a room of ghosts. The multiuser build is redesigned *and provisioned* — by evening a real canvas is served from Google Cloud off Firestore, with 150 ops written straight through two revision rollovers and not one refused. On the app side: a contrast audit, a changelog, a cursor that says who it belongs to, and the day's best bug — a lost CSS scope that rendered a room of live people as a room of absent ones. |
| **[21 Aug](2026-08-21.md)** | 24 commits | A vocabulary for asking. Slash commands arrive — the app never *runs* one, it writes down what was asked and an agent does the work. Plus the design system as a DESIGN.md, the `?` panel, and a layer scale that fixed four bugs which were all the same bug. |
| **[20 Aug](2026-08-20.md)** | 20 commits | Pointing at things: annotation as ink that knows what it is about, the selection riding along with the message, favourites, real thumbnails. And, on the other branch, a whole multiuser era on a designated host and a Firestore mirror — shipped this day, reverted the next. |
| **[19 Aug](2026-08-19.md)** | 9 commits | Agents get hands. Everything shipped web-only got a CLI verb, after renaming exposed an isomorphism bug: the web app moved the filename and the CLI did not. The rule got teeth the same day — a test that reads the CLI's registered commands and fails the build when the guide does not teach one. |
| **[18 Aug](2026-08-18.md)** | 12 commits | The Pen, the edge radar, and the decision that a directory is a project. Ink lands as an ordinary SVG item in world coordinates — the choice that later made merging drawings exact rather than approximate. |
| **[17 Aug](2026-08-17.md)** | 18 commits | Identity becomes an operation. Four stores and two client-side guesses become `actor.claim`, with one continuity rule applied by the single writer. The tool rail, the Zoom tool's tap/hold idiom, and ⌘K. |
| **[16 Aug](2026-08-16.md)** | 37 commits | The day it learned to be installed. Almost no features: version skew, two people on one machine, tests that outlive themselves, an install that unpacked to nothing. Also themes, and projecting a running localhost site as an item. |
| **[15 Aug](2026-08-15.md)** | 38 commits | Day one, and the spine is already here: one reducer for both clients, a single-writer daemon, actor-scoped undo, blob GC, presence with live cursors, comments, and an agent loop that parks on `wait`. |

## What goes in an entry

The headline change, the reasoning behind it, and the bugs worth remembering —
especially the ones whose cause was not where it looked. Then every commit
subject from that day, so nothing is hidden by the summary.
