# 13 September 2026

## The integrated continuation is locally green

The final source, after synchronizing with `origin/main`, passed
`npm test -- --maxWorkers=6`: 4,931 tests across 492 files, exit 0. Eighty
Firestore emulator tests were skipped locally, along with three existing
ACP/dispatch skips; CI requires the emulator. Workspace typecheck and build
passed. The entry measures 729,373 bytes against the unchanged 734,200
ceiling. The roadmap and phase records now describe the implementation and
its remaining acceptances together.

The first CI run passed all 499 files: 5,011 tests, with only the three
existing ACP/dispatch skips. Typecheck and grader selftests also passed, but
the release job hit its fifteen-minute budget during the metric selftests,
before either generated ref advanced. Tests alone took 752 seconds. The
last successful release had already taken 14 minutes 55 seconds. Release
now shares the PR job's twenty-minute budget; every check and test deadline
is retained. This gives the verified pipeline time to publish its result.

All ten standard built-browser journeys passed on the integrated features.
Separate real browser walks proved multi-home navigation, reference cards,
palette capabilities and competition creation/removal. The final touch
replay after the hook-stability fix again passed stages 0–2, including Chat
drafts, node navigation and touch presentations. The full suite had exposed
an omitted runtime API export, incomplete test teardown and fresh containers
returned by two hooks; those were corrected before this green run.

Inbox's four phases and embed's MCP phase are closed. Mobile's physical-phone
acceptance, hosted sharing's native Space gesture, operator's live dev walk
and competition's real-fighter, human, hosted and image proofs stay open.
The last native UI attempt found the Mac locked, with no browser surface to
drive; it did not replace that missing gesture with an assertion.

## Operator acceptance is prepared for the named people

The readiness pass independently reran 202 focused operator checks. The
hosted runbook names the accounts, devices, networks and storage observations
needed for each existing act, including stale proofs and content-origin
cache boundaries. Final terms wording is a reviewable unpublished proposal.
The emailed-link flow is authorized; the operator address and live sign-in
have not been supplied. No live operator configuration or terms page changed.

## Inbox reads the home that owns the answer

The home panel, in-app notification and CLI inbox now share authoritative
assembly. Each canvas is read through its own home with its admission and
operator refusal checks. A retained replica cannot supply an old success
after withdrawal, and an unavailable home cannot hide the other home's
answer. The visible page polls every thirty seconds without marking visits;
accepted arrivals clear only entries through their captured operation head.

The two-home proof exposed a mark written to the wrong home and an ignored
global CLI canvas selector. Marks and targeted prior reads now route by the
canvas named. Initial seen preparation is inside the poll's cancellation
and retry lifecycle, including its forwarded HTTP. Separate read scopes
share one identity repair. The existing refusal regression then caught a
claim awaiting its own repair promise: claims can repair their badge, but
cannot recursively repair their identity.

An authoritative response and a monotonic recency cache are different facts.
A legacy local mark of 99 must not replace a home that just answered 5, or
answered that it has no mark. The phone digest captures the exact targeted
response before the ordinary visit write; unavailable stays distinct from a
first visit. Accepted visit notifications retain the monotonic cache.

The switcher now works from home, lens and canvas. Recent leads, followed by
space headings without duplicates. Phone and covered views retain navigation
commands, so a command cannot silently change a desktop panel hidden behind
the current face.

## The phone opens in the conversation

Chat, Canvas and Agents are visit-local faces of the same record. The node
walk uses the existing spatial neighbors; its plan, conversations and agent
stage links do not mutate the canvas. Drafts survive tab and width changes,
and the phone does not overwrite desktop panel choices. Explicit message
references and eligible saved request roots get labelled current previews;
the frozen disclosure remains. Expanded descendants and author/time lanes
cannot pretend to be work a message produced.

The combined browser walk caught an Inbox button covering Submit and thread
links opening desktop state behind the phone. Inbox has a reserved header
slot and addressed conversations reach the visible sheet or Chat. Touch
presentations now use tap thirds and horizontal swipes; vertical text
scrolling and embedded controls keep their own input. Notes, exit and the
print route were exercised in both viewer and fullscreen. Physical-phone
keyboard, Safari toolbar changes and gesture feel remain acceptance work.

## MCP keeps the caller through the whole conversation

The server now exposes fifteen tools and canvas/Context resources. An
explicit session claims its own durable actor; calls use that identity
without replacing the machine's ambient person. Item and comment writes use
the existing API and operations. Feedback shares CLI addressing, resumes its
cursor through irrelevant traffic and advertises no presence on its own.
Saved request content retains its original bytes after later edits.

The independent review found two boundaries the first implementation missed.
A feedback timeout began after connection setup, leaving held identity and
admission requests outside its promise. The deadline now owns setup and
polling together; actual spawned stdio tests prove that cancellation closes
the held HTTP and never starts a late watch. A Context inheritance card
inside an excluded group also still read its source. Shared ancestor
exclusion now removes that edge before fetching, preserving visible
unavailable layers and the separate meaning of explicitly saved context.

## Setup keeps the credential it just redeemed

#284 was a persistence race, not a late claim. CLI setup and the daemon both
merged `identity.json`, in separate processes; either could replace the
other's newly saved fields. A process-local badge queue did not protect the
CLI's adoption write. Losing the home credential led to a fresh badge after
restart, correctly refused because the original badge still held the actor.

Replica setup now asks its local daemon to save the pass-returned person
beside its badge writes. Direct setup still saves only on its own machine,
where the badge writer is in the same process. A different held person stays
the default. Hosted and non-loopback callers cannot request adoption, and
the request never supplies arbitrary actor authority. Atomic writes preserve
private fields and file permissions. This fixes the setup path; it does not
claim a cross-process lock for every unrelated identity command.

The regression gates real CLI/daemon reads and writes over IPC, restarts the
replica, and mints at the authoritative home. Restoring the old writer
placement makes that final mint fail deterministically. No retry or relaxed
claim door hides the missing credential. An older daemon that cannot confirm
adoption produces an explicit restart error; setup never falls back to the
unsafe writer arrangement.

## Competition arenas use the current canvas model

The competition implementation from PR #264 now creates an explicit forest
of groups in one bounded operation. Scoped design systems, wait filters,
hand-in and the curtain share core membership instead of inferring belonging
from overlaps. Enrolment prepares the rc directory and configuration before
publishing the actor another process can immediately dispatch. Conditional
rollback cannot overwrite a concurrent update; lost receipts keep the
configuration the remote writer may already have accepted.

The picker and module CSS load on demand. Generic module hosts and the print
route also became lazy, keeping the existing entry ceiling. The conductor
walked both Chat and keyboard entry, actual pointer selection and arena
creation, then the print/HTML/PDF export. Source/runtime removal and restore
preserved three synthetic arenas and the exact operation log. The phase
record keeps real-fighter quality/cost, hosted voting and container-image
removal as separate unfinished acceptances. Blind bouts remain refused.

## A shared address does not make a hosted directory

Hosted discovery now lists recorded admissions and named invitations, including
groups, spaces and the creator's floor. A link still admits somebody who has
its address, but the home no longer supplies that address in an unrelated
person's list. The change adapts #291 and closes its explicit
`reach=admissible` exception. Homes, presence whereabouts, unscoped watches
and takedown lists use the same boundary. A laptop's loopback shelf and a
replica's admission-only listing retain their separate meanings.

The less obvious failure was grant ranking: discarding a link after selecting
the strongest row also discarded a valid, weaker named invitation. Discovery
now removes link candidates before ranking. The entry door remains the same.

Both clients can create directly inside a space. The web's Space selector and
`isocan canvas create --space` carry the existing birth envelope, so ownership
is checked and space access exists before the canvas can acquire a birth link.
No new operation or later move is needed. A granted owner can create; an
editor cannot; a refused form keeps its title and selected space.

Independent verification on the integrated tree passed 4,788 tests and
typecheck, with 83 emulator tests skipped. All ten built-browser journeys
passed. Separate browser/daemon walks proved discovery and creation/refusal;
the native select gesture is still unproved because headless Chrome did not
consume its injected keys. The submitted value was exercised through a DOM
selection followed by real pointer input, and the phase record says exactly
that.

## The delete proof selects before it stalls the network

The delete journey clicked the center of a small text item and entered its
reading control instead of selecting the item. It now selects exposed item
chrome, verifies selection, then stalls operation posts and proves local
deletion while a post is actually held. Removing optimistic deletion in a
mutation check makes that assertion fail. This repairs the proof without
weakening the network condition it exists to hold.

An atomic group insertion also made a card say "changed a canvas group" for
the ordinary act of adding an item. Activity summaries now derive that act
through one core helper used by the card stamp, lens, timeline and old-file
stamp repair. The recorded operation remains the canonical group change;
its deletion inverse and explicit grouping acts keep their own meanings.

## A bound of 0 cannot be tightened, so the report says what it is 0 past

The performance persona's *bytes past the last size somebody agreed to* has one
property no other goal has: its bound is the constant 0 by construction. The
number is the OVERSHOOT of `CEILING` in `scripts/bundle-ceiling.mjs`, not a
size. Every other goal gets re-asked the night somebody tightens its bound —
that is what makes tightening one an act with a consequence — and this one had
no bound to move, while the thing the decision is actually about, the agreed
size, appeared nowhere in the report.

So an `accepted` given at one ceiling went on covering creep against every
later one. Measured against the real queue: the four answered nights of 8–11
Sep read 3899, 1169, 4706 and 5799, so the smallest yes was 1169 and the
covered window 1169 x 1.1 = 1285. A night reporting 1,200 bytes past a ceiling
**12,306 bytes higher** — the raise of 11 Sep — sat inside that window and
never reached the queue. Twelve kilobytes of agreed growth, and the guard that
exists so a correct report cannot be ignored could not see it.

The finding carries the basis now. A persona goal may declare `against:`, the
command that prints what its debt is a debt against; `scripts/bundle-ceiling.mjs`
prints `CEILING` when run as a program, so there is no second spelling of the
number the hard gate enforces. A nightly finding reads *bytes past the last size
somebody agreed to is 5295, past 0 of 721200*, and `isocan persona ls` and the
board's panel print the goal as *at most 0 of what that command prints*, rather
than a 0 that reads as an absolute.

**Where it is weighed is the half worth keeping.** `findingKey`'s `goal|bound`
stays the one definition of *which question* — the basis rides beside `value`,
as a reading, and `findUnanswered` scopes an answer's coverage by it. Folding it
into identity was the obvious move and is wrong: for this goal the treadmill IS
the ceiling moving, so five nights of one question would have read as five
questions asked once, deleting the only signal that says a bound wants enforcing
rather than another answer. That signal is what produced the 11 Sep raise.
`test/review-queue.test.ts` holds both halves, and `docs/reviews/lessons.md` #55
states the shape: a bound of 0 on a derived number carries no identity, so the
thing it was derived from has to travel with it.

## Canvas groups move, resize and make room for labels

Canvas-groups phase 3 closes the transform and insertion work on both clients.
Corner previews include every descendant and attached mark; cancellation drops
only that gesture's preview, and the accepted operation carries all resolved
boxes. Frame-only sizing and fitting preserve member geometry. Saved title,
brief and grid reservations drive the same placement helpers in the CLI and
browser. Add, file, text, site, document, module and sprint producers carry
membership into insertion rather than issuing a second reparent operation.
A first sandbox transcript inherits its program's group; later versions keep
the transcript's own parent, verified with an actual fenced program run.

The bounded insertion/content effects require canvas-groups-v2. Original v1
history still replays, including dense grids whose saved cells cannot hold the
new full spacing. Those grids disclose insufficient room and compress only
rendered inter-cell gaps; new layout requests enforce the full reservations.

The browser found failures the geometry tests could not: a keyboard timer
appending an empty transform after its nudge had already completed, delayed
receipts interfering with another gesture, and a file upload joining the new
canvas's queue after navigation. Queue and completion ownership now includes
the original canvas and gesture. The old history-view write guard remains
intact, as the full suite required. A canvas with 1,000 documents also exposed
unreadable previews and idle sprint subscriptions; those now stand down while
retaining selectable frames and image/video visual faces.

The conductor's final local suite passed 4,718 tests with real Firestore
emulator cases enabled; the bundle check skipped after rebase was rebuilt
and checked separately, leaving three existing ACP/dispatch skips. Workspace
typecheck passed.
Real HTTP, CLI and browser walks covered numeric transforms, both annotation
insertion orders, conflicts, all resize corners, transport ordering, nested
drops and exact undo. The 1,000-card nested fixture reached the browser 47 ms
after seeding completed, mounted no document previews at overview zoom and
fetched no blobs during movement. Pointer updates measured roughly 80–125 ms;
this is a recorded limit, not a claim of smooth 60-fps motion. The final entry
bundle is 721,172 bytes after deferring the Add form, content fitting and
operator-only refusal helpers; the 721,200 ceiling records that cost without
changing the long-term goal. After syncing upstream, delayed requests were
also checked against terminal access refusals: the refusal page stays visible,
and API badge recovery preserves the door's definitive answer.
Frozen request context and lifecycle were next in canvas-groups phase 4, closed below.

## Group requests keep their context, and copies keep their structure

Canvas-groups phase 4 freezes a message's complete membership and versions at
the writer revision the sender reviewed. Browser disclosure, CLI request/content
reads and read-only MCP use the same manifest. Exclusions remain visible, and
source and distinct visual faces survive deletion, garbage collection, restart
and native backup within the canvas deletion boundary. The real retention walk
caught fallback metadata describing an HTML visual as its Markdown source;
omitted fields now come from the visual blob itself before the writer freezes
anything. Snapshot adoption validates this retained metadata before replacing
state. Literal v2 readers drop the new record, so v3 is the honest boundary.

Copy prepares every source/visual face before one bounded subtree creation,
remaps only declared references, preserves deliberate overlap, and refuses the
whole request if any required face is missing. Trash inspection uses the same
cohort resolver as restore, which leaves independently restored or re-deleted
children alone. A second daemon verified the native round trip.

Independent review caught message acceptance erasing newer text and queued
message/restore forms staying locked. Completion now belongs to the original
canvas and operation, and actual browser transport ordering verified both
accepted and refused reconnect paths. The 1,000-card context walk exposed all
1,011 entries across 21 pages with no document previews in the disclosure.
The full suite passed 4,784 tests with three existing skips; typecheck and build
passed. Lazy context/Trash panels and a writer-only core split reduced the
initial entry by 11,339 bytes to 734,159. Legacy conversion and normal creation close in canvas-groups phase 5 below.


## `--names` exists wherever a guard says it does

The copied-rules guard has ended its failure message with "Which ones: node
scripts/measure.mjs copied-rules --names" since the day it was written, and that
flag did not exist. `measure.mjs` dispatched `--names` from an `if` on one
metric's name, so the command printed the bare count and exited 0 — handing
somebody whose commit had just reddened on 59 copied rule bodies the number 59.
`undocumented-exports` carried the same dead instruction.

`--names` is a `names()` beside a metric's `take()` now, off the same scan as the
count, and a metric that has none exits 2 instead of printing a number. Three
metrics answer it; `--list` marks which. `copied-rules --names` prints each
duplicated body with the selector that declared it first and the ones that
repeat it, biggest family first — today 59 copies over 38 bodies, the largest
being five controls agreeing about what the accent looks like.

The guard against the next one is `test/measure.test.ts`, which greps every
`measure.mjs <metric> --names` out of `test/` and `.agents/personas/` and runs
each, asserting it exits 0 and answers with more than the count. Recorded as
lessons.md #56: a comment that names a command is a claim, and nothing runs it.

## Existing areas become groups, and new canvases start there

Canvas-groups phase 5 closes the project. Conversion previews one stable
owner per item, follows annotation targets, preserves ordinary geometry and
expands frames for declared label repairs. Live and trashed areas convert in
one bounded v4 operation. Historical v1–v3 replay remains unchanged. Literal
old browsers and replicas receive no unsupported state, including on retry;
new clients show upgrade guidance without discarding queued work.

The boundary applies to every actor, including the converter's prior redo,
and ignores ordinary gesture labels so conversion stays independently
undoable. Rollback names surviving group-dependent live, trash and history
debt. It retains later primitive text and discussion. Ordinary root creation
Undo still removes a card after a collaborator moves it; grouped structural
changes retain their conflict checks.

Independent review and real walks found three more crossing points. Uploads
must capture mode before waiting for bytes; migration refusals must retain
that intent across reload; and export must capture snapshot and complete
history at one revision. Actual conversion and GC races proved the backup
checks. Firestore requires an explicit deletion for an absent boundary under
its merge semantics, and replica adoption must advertise its capability.

After integration with the latest main, the conductor's full suite passed
4,860 tests with three existing skips and all 477 files green, with Firestore
enabled. Typecheck, build, real old-client and two-daemon backup walks passed.
The final integrated entry is 733,714 bytes after Context and Personas became
deferred panels with retained state; the ceiling
stays 734,200. Normal creation, aliases and people-sharing commands were
walked again on the completed feature.
