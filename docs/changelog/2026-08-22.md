# Two disks under one engine, and a colour audit

**2026-08-22** · 29 commits · 204 files · +14,910 · Dimitri Glazkov, Dion Almaer

The multiuser build returns with a different architecture — Store as an interface, CloudStore behind it, identity as a desk of mechanisms — while the app side got a full contrast audit and the research directory.

## The multiuser build, designed then started

- The journey written down as the ideal, then a coherence pass over it; the identity desk inventoried mechanism by mechanism — the badge, grants and attestations, actor binding, registry scope, the sponsor rule.
- **The store seam**: `Store` is an interface and `FileStore` is behind it — which is what makes a second disk possible without the engine knowing.
- **CloudStore**: the same engine, a second disk under it. Actor binding turns rules that looked like manners into enforcement.
- The conductor protocol: a build session spawns one subagent per phase and verifies the named proof itself rather than taking the subagent's word.

## The app side

- **A colour audit in both themes.** Every text element measured against what is painted behind it: 15 light and 13 dark failures before, 0 and 0 after. The reported bug was a `<button>` with no `color`, painting the UA's black.
- Sixty-three literal colours became tokens, and a test holds the line.
- Fitting aims at the canvas you can see, so nothing lands under an open panel; F frames the selection, V stays Select, S shows the version stack.
- `docs/research/` — findings written down where they can be found.

## Every commit that day

- Black text on a dark card, and the ten other colours that failed
- Merge: cloudstore lands upstream
- Research lands in the repo, and "@Di" reaches Di again
- CloudStore: the same engine, a second disk under it
- Actor binding: the rules that looked like manners become enforcement
- The badge: the door hands one out, every surface carries it
- The store seam: Store is an interface, FileStore is behind it
- The placeholder says what the channel is
- Fitting aims at the canvas you can see
- F frames it, V selects, S shows the stack — and the title stays on the item
- One name for the thing, and one canvas out of the fixtures
- The conductor protocol, and a trailhead for clean sessions
- The architecture and the phases: the map and the walk
- Coherence pass three: five seams closed in the design docs
- The sponsor rule: who may re-vouch an agent, and four smaller seams
- Offline birth: the promise, the adoption, the twins, the move
- The innkeeper chosen out loud, and a coherence pass over the journey
- Registry scope: mechanism 10 — three facts, three scopes
- Actor binding: mechanism 5 — each hop vouches for what it can see
- Sanity pass: the desk squares with itself
- Grants, attestations, and the door: mechanisms 3 + 2
- The badge: mechanism 1 of the identity desk, designed
- The identity desk, inventoried
- Scene 0 gets a preamble: the front page is a door too
- Consistency pass: the journey squares with itself
- Scene 7: always-answerable, not always-on
- One door for people; the service worker does offline
- The canvas teaches its own escalation
- The multiuser journey, written down as the ideal
