# Identity becomes an operation

**2026-08-17** · 18 commits · 126 files · +4,493 · Dimitri Glazkov, Dion Almaer

Who an agent is stopped being four stores and two client-side guesses, and became `actor.claim` — one continuity rule, applied by the single writer. Plus the tool rail, the Zoom tool, and ⌘K.

## The identity problem, solved in the vocabulary

- **Naming yourself is an operation.** The registry's history is an oplog like everything else's.
- "One continuity rule, not one per client" — two clients had been re-implementing the same rule over different storage.
- "A name is taken by everyone the canvas answers to, not only by who is on it": a name stays taken after its wearer goes quiet.
- "Two agents in one directory are two people", and a session pointer belongs to one agent.

## The canvas gets its furniture

- Figma-style layout: content on top, a tool rail on the right, zoom bottom-right.
- The Zoom tool — tap to latch, hold for momentary — and the tap/hold idiom the Pen later borrowed.
- **⌘K**: a friction-free lane to your emissary from anywhere on the canvas.
- A cursor spotlight, so the grid lights up where you point.

## Every commit that day

- A cursor spotlight: the grid lights up where you point
- Two cursors on one point, and a face that outlived its agent
- A session pointer is one agent's, like everything else
- The identity files already in the wild
- One continuity rule, not one per client
- Naming yourself is an operation
- A directory cannot tell one agent from another
- A name is taken by everyone the canvas answers to, not only by who is on it
- A name is one agent, and a directory name is nobody's inheritance
- Three slots, told as one story
- Two agents in one directory are two people
- Figma-style layout: content on top, tool rail on the right, zoom bottom-right (#54)
- The Zoom tool: tap to latch, hold to zoom a region
- Fix rail shortcuts: V selects, Space is a momentary grab
- Three edges: content on top, tools on the right, zoom bottom-right
- A friction-free lane to your emissary: the ⌘K command bar (#53)
- A friction-free lane to your emissary: the ⌘K command bar
- Turn the lights off: light, dark, and system themes (#52)
