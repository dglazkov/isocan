# 15 September 2026 — Design partnership from request through review

Design-partner phase 3 makes creation, source reads, audits and Files notices use
one governing-system result. Group, ancestor, canvas and permitted inherited
systems keep their scope. Explicit exemption removes the requirement to write a
system; it does not hide an incumbent. The widened request binding is negotiated
with both daemon generations, including an actual archived prior-daemon probe.

Three authored native references demonstrate different compositions: receiving
stock, reading a field guide, and reserving a place in a synthetic repair
workshop. Their actual tasks, states, keyboard behavior and mobile layouts were
walked in Chrome. A second screen reads browser-authored accepted treatments
through a distinct CLI agent and preserves its measured controls. A constrained
repository extends its real component and tokens against its existing server.
These are engineering and craft-reference results; the independent design-quality
and partnership study still belongs to phase 7.

Verification changed the implementation. Malformed documents must be validated
before their retry journal becomes immutable. An accepted receipt survives a
later read failure, and a generic operation ID cannot substitute for its actual
payload and actor. Different governing documents need archive-before-close
recovery. Running HTML in its native origin did not prove the embedded preview:
actual opaque frames exposed blocked native forms and a history navigation error.
The preview now keeps its isolated origin, working local focus/navigation and
honest temporary-state labels. Legacy areas cannot guarantee a scoped add without
possible resize/shift acts, so the library requires groups for new creation.

Integration also repaired the incoming bench release failure: seven unused
implementation exports now remain local, with its actual CLI/core/web behavior
and declaration output preserved. The existing API ceiling stayed in place.

The final source and complete local journey passed full fast, typecheck, build
and strict emulator gates. The initial entry remains 722,646 bytes under the
unchanged ceiling. Phase 4 adds the alternatives and adopted decisions described below.

See [phase 3 verification](../projects/design-partner/verification/phase-3.md)
for commands, source identity, evidence, limits and the course-changing findings.

Phase 4 closes with real structural and visual alternatives. Agents publish exact
versions, hypothesis, tradeoff and recommendation; the person tries them, chooses,
delegates or asks for a specific revision. One canonical act adopts compatible
bytes and records the decision, with one conditional paired Undo. Human words,
agent recommendations and native-conversation reports remain distinct. Corrected
preferences and later screens retain the effective rationale and exact history.

Browser verification changed ownership and recovery. The phone breakpoint had
destroyed a button-owned comparison; its stable page host now preserves the exact
running frame and focus. New publications cannot switch the source being reviewed.
A missing publication still exposes the local pending action, and Retry never sends
until its original identity is saved. Archive verification caught a generic edit
reusing a decision's operation ID after GC; the common submit path now reserves
those identities too. Retained source and visual bytes survive pruning and restart.

The conductor's final integrated journey passes all twelve checks, including
actual tasks and correction at wide and narrow widths. Full fast, typecheck and
strict emulator gates pass with unchanged bundle/export ceilings. These are
engineering and authored craft proofs; independent generated-design and partnership
quality still require phase 7. Phase 5 completes the local candidate below.
See [phase 4 verification](../projects/design-partner/verification/phase-4.md).


Phase 5 completes local Scenes 1–6 as one continuous journey. The same request
keeps its answers, uploaded sketch, governing system, working alternatives and
accepted rationale through two agents, then records separate source, task and
craft observations. Real Chrome catches a broken Save and phone overflow; two
reserved repairs fix and recheck them. Static analysis still reports its coverage
limits. Missing inspection finishes as an honest draft, and a verifier request
never claims work that has not happened.

One canonical `design.repair` preserves exact target/context identity and one
conditional Undo. Actual writer and archive probes forced full metadata checks
on restoration, fixed a pre-existing grouped-Redo ordering bug, and preserved
completion through its exact canonical edge. Missing or conflicting report history
means unknown budget; original request/run bindings prevent unrelated lost history
from blocking a new task. Retained final evidence survives actual GC and restart.
Verifier discovery uses real live canvas actors, current tool offers and existing
wake authority. Browser drafts preserve original identity through refresh and
uncertain delivery; the review remains open across desktop/phone remounts.

The first full gates found an eval consumer that omitted the original repair
basis and a browser helper duplicating an API route. The changed-candidate and
metadata-race regressions now prove the real consumer, and the actual six API
transports share their route with the fault helpers. Corrected fast, typecheck,
build, strict emulator and independent browser checks all pass, with unchanged
export and bundle ceilings. No paid eval ran and prior pilot evidence stays intact.
See [phase 5 verification](../projects/design-partner/verification/phase-5.md).
Optional Impeccable compatibility is next; independent quality and partnership
evidence still controls default rollout.


Phase 6 closes with optional adapted Impeccable guidance for starting, critique
and finishing. One shared reader preserves the brief, settled answers, exact
references, governing system and actual chosen direction across API, CLI and
browser. Exports go to a new folder, preserve authored working files and reconcile
system edits through ordinary conditional operations with one Undo. Source drift
visibly invalidates an open view without replacing its captured context.

The pinned package and real native launcher were inspected and probed. Its context
loading works; native design playbooks remain unsupported because their ceremony
and capabilities exceed this integration. Guidance adds no questions, mutation,
repair allowance or default enablement. Actual attributed use fits the existing
review and receipt, and opening the panel records no inspection.

Independent proof exposed the difference between recommendation and human choice,
runtime actor DTOs and closed saved shapes, and completion attribution versus fact
authorship. These are corrected and recorded as lessons 80–82. Full strict checks,
fast suite, typechecks, build and real browser/CLI walks pass. An initial unchanged
grid-test timeout under concurrent heavy gates passed with the same timeout when
the fast suite ran alone. The final entry is 742,315 bytes under the unchanged
ceiling. See [phase 6 verification](../projects/design-partner/verification/phase-6.md).
Phase 7 now measures independent output quality and partnership before rollout.


Phase 7 now has verified preparation for measuring the partnership. Actual
frozen baseline and candidate builds feed the same briefs through both entrances.
One bounded native runner records reservations and uncertain charges; final
artifacts receive independent task checks at both frozen widths, and three
blinded review packets retain missing outputs and failures. Six counterbalanced
participant instruments stay empty until real people use them. No provider was
called, and the shared workflow remains opt-in.

Real tool walks changed the evaluator: input replacement and native Enter must
work before judging an app, and a checked file cannot change during CLI capture.
The final runner orders file/CLI/browser work and checks transitive local assets.
The isolated study browser handles task pages and the fixed connected fixture;
the bound CLI handles canvas state. Local rollback and public dev identity pass,
while authenticated hosted acceptance remains explicitly owed.

Full fast, typecheck, build and strict Firestore/process gates pass. Phase 7 is
PART-DONE: the concrete next request is a $40 API-equivalent smoke ceiling and
compatible isolated authentication, followed by independent ratings and the
separate six-person partnership study. The [verification record](../projects/design-partner/verification/phase-7.md)
separates engineering evidence from the measured quality still required.


Landing also reproduced an existing identity race behind an incoming release
failure. Successful replica setup could lose its CLI badge when the daemon's
home-badge write replaced a stale identity snapshot. All shared identity writers
now hold the same physical-home lock, including human-name updates. Independent
before/after schedules prove the original refusal and restored next-command
custody after both daemons restart; corrupt data and foreign locks are preserved.
The final combined tree passes the full gates. Frozen design-study runtimes and
its provision requirements remain unchanged.


The [execution retrospective](../research/2026-09-15-design-partner-execution.md)
now preserves the phase timeline and aggregate timing evidence. Lessons 87–88
and the conductor apply its practical findings: cheap staged-source checks
before broad gates, one heavy local verification at a time, compact phase
handoffs, and integration before the final source freeze. Required full tests,
typechecks and independent journeys remain. A future scheduler and shared proof
infrastructure are recommendations; no speedup or design-quality result is
claimed by recording them.
