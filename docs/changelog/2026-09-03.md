# 3 September 2026 — Roles reaches prod

One thread for this session: the roles project's last phase, closed on
isocan.io.

## Roles phase 6: nothing changed for anyone else

Prod was deployed at `5475a35` (main at 08:32 UTC, every roles phase
included) and journey 8 was walked on it with Dimitri's badge and a fresh
door badge as the stranger. Eighteen canvases existed before the deploy.
Three whose link row carries no rung admitted the badge with no
capability on the wire — editors, as every grant from before the field
meant. The one whose link had been set to `view` before the deploy gave
the stranger `view`, so the deck still opens. `GET /api/spaces` and
`/api/groups` answered empty and the canvas list drew no heading, because
a home with no space is the home it was. Recorded in
[roles/phases.md](../projects/roles/phases.md): the sha, and that every
multiuser and multi-identity test passes at `b68035a`.

The old-client and old-home halves had been walked the night before on
dev and against a pre-phase-1 build from the release branch, because prod
had already moved past phase 1 by the time anyone looked. One finding
stands open from that walk and is phase 3's: a pre-phase-1 home drops
`bars: true` and writes an ordinary invitation.
