# Phase 5 verification — shared review and bounded repair

Phase 5 CLOSED on 15 September 2026. The conductor independently verified the
continuous journey, canonical history/recovery and every integrated gate. No
quality or rollout gate is closed by this engineering record.

The execution baseline is phase 4 commit
`9631383f9ba4c6f6b561cb5f80293969ad20c06a`; the phase started at
08:47:29 UTC on 15 September 2026. Mechanisms were committed before their
implementation. After integrating upstream `b2813bbe` without conflicts, their
rebased identities are `20710259` (shared run and canonical repair), `99dc6d44`
(two consequential questions plus optional upload), `b7d12947` (conditional
restoration and source coverage), `99029ae2` (dependent grouped Redo order),
`e4f10101` (exact completion continuity), `bb40f487` (unknown budget and conflicting
history identity), and `a60457aa` (scoped discovery and actual live verifier).
No unrelated work in the original checkout was included.

The [source manifest](phase-5-sources.json) records the final 81 implementation
files and production entry. The workflow remains opt-in. Authored synthetic
repairs, tool observations and craft examples establish correctness and task
behavior; generated-design quality and real-user partnership remain phase 7.

## Independent before-state evidence

The conductor archived and separately loaded genuine phase 4 source and its
final built entry, with every @isocan workspace resolving archived code.
`/tmp/isocan-design-partner-execution/phase4-daemon-baseline/baseline-proof.json`
records source `9631383f`, source archive SHA256
`fc503d51074e8948bb2fc8d0e97129e168fe10d7700711565a270f4bb53dc97c`,
and `index-DxZeQHrj.js`,740427 bytes,SHA256
`f3bc87d798f97250f0b79b4a511dc88f282b688dae3fc7b654825a47fe70c647`.
These are actual old implementation bytes, not the candidate labelled with an
older SHA. Installed external dependencies are shared; workspace source is not.

`node --import tsx /tmp/isocan-design-partner-execution/phase5-conductor-old-daemon.mjs`
exited 0 (`phase5-old-daemon.log`, evidence directory ending `P2HdLI`). The real
old daemon reports `unknown operation: design.repair`, HTTP 400, with no target,
thread or sequence change. Its ordinary metadata edit still returns 200.

`node --import tsx /tmp/isocan-design-partner-execution/phase5-conductor-old-repair-gap.mjs`
exited 0 (`phase5-old-repair-gap.log`, evidence ending `ezNDxP`). A genuine
version-conditional item.edit accepts at sequence 5, but its selected-target
request changes from current to stale; completing that exact brief is refused.
The saved repair remains in place. This reproduces the context gap that requires
an explicit canonical repair transition; it does not prove the later fix.

## Boundaries

Synthetic authored repairs and actual tool observations can prove orchestration,
state, custody and task behavior. They cannot measure generated-design quality,
independent human craft preference or partnership value. Those remain phase 7.
No paid model/provider/browser service or real people messaging is authorized.

## Independent candidate evidence collected before final gates

All commands below use `node --import tsx /tmp/isocan-design-partner-execution/`
and exited 0. The final integrated journey and parity reruns are recorded below;
individual probes identify their earlier source boundary.

| Scratch command / log / evidence suffix | Observed result |
| --- | --- |
| phase5-conductor-task-runtime.mjs / phase5-task-runtime-1.log / fn5XxU | Nine actual Chrome controls: all four Save/phone-defect combinations at 1280 and 390, plus real connected incumbent stock GET, held POST, failed-save recovery, retry, correction PATCH and reload GET. Component/token source remains the supplied incumbent. |
| phase5-conductor-audit-browser.mjs / phase5-audit-browser-2.log / osRdcc | Seven source counterexamples on add/revision: full API/CLI JSON parity and actual CLI text; browser source hashes, governing version, rules, counts, diagnostic codes/values/explanations, candidates, prerequisites and coverage agree. First production build, final rerun owed. |
| phase5-conductor-writer.mjs / phase5-writer-5.log / 09BqbR | Seven actual daemon protocol checks: two reserved canonical repairs continue exact selected context, retry/Undo/Redo, metadata/content drift refusals, persisted-but-unsent retry, conflicting intent and completed task/current draft receipt. Task/craft observations explicitly unavailable in this isolated proof. |
| phase5-conductor-restore.mjs / phase5-restore-1.log / xvx8lg | Newer content, title, description or properties refuse repair Undo and Redo without changing the snapshot or consuming the candidate. Removing the teammate edit permits original restoration. |
| phase5-conductor-grouped-redo.mjs --baseline / phase5-grouped-redo-baseline.log / 57FOOa | Actual phase 4 daemon reproduces first-version final state after Undo/Redo of two dependent ordinary edits. |
| phase5-conductor-grouped-redo.mjs / phase5-grouped-redo-candidate-1.log / iMQblK | Correct original forward replay order ends on the second version. No repair-specific workaround. |
| phase5-conductor-retention.mjs / phase5-retention-1.log / nGkMwi | Remove report, empty trash, actual GC and restart retain the exact final source report through the current draft receipt. Orphan bytes are swept; incomplete prior report history cannot progress. |
| phase5-conductor-cli-finish.mjs / phase5-cli-finish-1.log / F5Lvtp | Actual CLI reentry after finished report and canonical completion adds only the missing receipt; same epoch. Repeat returns that receipt's original accepted operation and makes no write. |
| phase5-conductor-history-isolation.mjs / phase5-history-isolation-1.log / pTo8rT | Reproduced the old recovery bug: one swept report blocked an unrelated current task's first review. |
| phase5-conductor-history-isolation.mjs --expect-available / phase5-history-isolation-2.log / 9A5YBA | Original canonical request/run discovery bindings isolate old unavailable history. Its own run stays blocked; a different current request can start. The earlier receipt still owns its exact retained report bytes. |

Conductor-run focused regressions: server repair/Undo tests, 2 files/23 tests
(`phase5-root-core-tests.log`); API review/history/offer, server questionnaire,
actual CLI review and web journals/repair, 7 files/51 tests
(`phase5-root-review-tests.log`, `ISOCAN_DEEP=1`). These exercise actual daemons,
HTTP/WebSocket/second-daemon relay, live registry/presence/wake authorization,
concurrent reservation and archived operation identity. Synthetic tool declarations
in API tests establish transport semantics only; actual Chrome observations are
separately named above and in the continuous journey.

## The continuous browser journey

The conductor independently ran `node --import tsx scripts/journey-design-review.mjs`
against the corrected production build: exit 0 including teardown, all ten
grouped checks (`phase5-corrected-journey.log`). Evidence is
`/var/folders/63/4k_ydj554mz6_56j2hpg7jcr0000gn/T/isocan-personal-journey-hBkq6v/proof.json`.
The earlier integrated run passed all ten grouped checks (`phase5-root-journey-final.log`,
`isocan-personal-journey-pj6Anb`) before the final consumer/route correction.

The actual continuous task starts from a selected Acme brand and sparse canvas
message. It asks two consequential design questions and offers one explicitly
optional sketch upload in the same batch. The exact upload survives failed
answer submission and refresh; another agent's comment does not resolve it.
Both working wireframes execute receiving and correction before a real browser
human adopts one. A distinct agent reads the accepted direction and system,
then extends the second screen without repeating settled questions.

The initial review observes a broken Save and phone overflow in real Chrome.
Repair one restores Save and uses the existing design tokens; the narrow-layout
obligation still fails. Repair two fixes the phone layout. Each uses a reserved
pass and exact canonical context, with fresh observations on its resulting
version. Source coverage remains explicitly unsupported for unresolved shorthand
and executable styling; the browser task result never relabels it as fully
analyzed. The final receipt preserves those limits alongside passed task/craft
observations and their actual author. No independent human aesthetic rating is
claimed.

The same journey covers output/governing drift and restoration, invalid and
no-op candidates consuming both attempts, refusal of a third attempt, and an
honest draft when browser evidence is absent. Existing Design check edits real
CodeMirror source, survives a before-write HTTP 503 and refresh, recovers an
actual committed 408 from the full canonical receipt, adds one version and
performs one real browser Undo. The incumbent connected app uses its original
component and tokens against actual stock GET, receipt POST, correction PATCH
and reload GET at phone and desktop widths.

An actual Chrome self-probe, real native presence and held rc connection support
the verifier offer. Missing capability, expiry, stale output and unauthorized
wake remain ineligible. Requested/Picked up does not imply inspection. Failed
handoff preserves its original intent across refresh; actual acceptance followed
by an unavailable read remains accepted/unavailable, with no fresh resend action.
Identity changes cannot transfer another actor's pending repair.

The conductor inspected wide and 390-pixel screenshots of the actual repaired
receipt and review panel. The receiving hierarchy, evergreen controls, focus and
10/20/8 saved quantities remain legible. A squeezed Refresh label and panel state
lost at the phone breakpoint were found and fixed: wrapping follows the panel's
width, and scoped open/run state survives remount. Later publications do not
silently replace the selected review. These are bounded visual observations.

The final independent add/revision parity run passed 14 checks
(`phase5-corrected-audit.log`, `isocan-design-request-conductor-hJnMzc`).
Full API/CLI JSON, actual CLI text and the production browser agree on all
seven source counterexamples, exact source/governing identities, rule version,
findings, explanations, candidates, prerequisites and coverage.

## Integration failures and correction

The first full fast and strict runs failed the same two tests. Fast:
561 files / 5,699 tests passed, two failed tests, 108 skipped, 184.86 seconds
(`phase5-final-fast.log`). Strict: 617 files / 6,272 tests passed, two failed
tests, three skipped, 449.58 seconds (`phase5-final-strict.log`). Typecheck and
both actual browser walks passed on that source. The phase stayed open.

The design-lint eval consumer omitted the newly required original audit basis.
The apparent invalid-candidate error concealed a larger regression: changed
valid candidates would also refuse, while an unchanged clean control bypassed
repair. The runner now passes its original capture; guaranteed invalid source
preparation reports candidate rejection, and missing evidence stays unavailable.
The real consumer regression proves a changed candidate saves one canonical
repair without policy changes, an empty candidate makes no repair, and an
intervening description edit refuses the original capture. No paid model ran,
no corpus/threshold changed, and the separate pilot evidence was not rewritten.

The browser fault observer duplicated an operation route. One documented API
constant now serves the six actual operation transport methods and both fault
helpers. No literal was disguised and no boundary guard was waived.

## Final integrated gates and surfaces

| Command | Observed result |
| --- | --- |
| `npm test -- --maxWorkers=6` | Exit 0; 563 files / 5,702 tests passed, 108 skipped; 190.20 seconds; `phase5-corrected-fast.log` |
| `npm run typecheck` | Exit 0, all workspaces; `phase5-corrected-typecheck.log` |
| `npm run build` | Exit 0; 5.56 seconds; `phase5-web-build-boundary-final.log`; entry agrees with the manifest and served journey |
| `node /tmp/isocan-design-partner-execution/strict-test.mjs phase5-corrected` | Exit 0; files: 619 passed (619); tests: 6275 passed, 3 skipped (6278); 450.00s; `phase5-corrected-strict.log` |
| `node --import tsx scripts/journey-design-review.mjs` | Exit 0 including teardown; all ten grouped checks; `phase5-corrected-journey.log` |
| `node --import tsx /tmp/isocan-design-partner-execution/phase5-conductor-audit-browser.mjs` | Exit 0; 14 actual parity checks; `phase5-corrected-audit.log` |

The strict wrapper owns and stops a local Java 21 Firestore emulator and runs
`npm run test:ci -- --maxWorkers=6`, including deep/emulator and built-bundle
checks. Paid/model experiments stay opt-in. Final documentation, lesson, CLI
surface, export and bundle checks passed 43 tests in eight files in 4.64 seconds
(`phase5-final-docs.log`). Conductor lint, both generators, lesson collision and
diff checks pass. The decisions generator also catches up already-written lessons
55–76; their underlying text is unchanged.

The corrected source manifest was captured with every intended implementation
path staged and identical to its working bytes. Its initial entry is
`index-BWh8ApdD.js`, **742,261 bytes**, SHA-256
`5c909d5b0a013aaf7d436e95f0a6e90d9747cb7da9406a27dc177727c63550f6`.
The bundle ceiling remains **743,900 bytes**. Unused exports measure **39**
against the unchanged **39** ceiling; undocumented exports measure **330**
against the unchanged **331** ceiling.

The one new public act is `design.repair`, materializing one conditional item
edit and inverse. CLI `design review` exposes read/start/record/reserve/offer/
handoff/finish/retry; the existing repair command consumes the same prepared
operation. Agent guide, runtime loader, API and lazy browser review share the
procedure. Deterministic source analysis remains the existing resolver/auditor.
Core/server own authority, history and exact continuation; ordinary versioned
artifacts hold run/offer evidence. No second workflow store, private interview,
HTML admission marker or new external service was introduced.

All six repository surfaces are accounted for: operation vocabulary, CLI act,
agent guide, shared logic, README and meaningful tests. Agent publication stays
an API/CLI act; the web presents review, recovery and verifier handoff without
requiring a manual schema form. Actual browser interactions are named above;
contract fixtures are not offered as visual proof.
