# Projects

One directory per body of work. A project holds everything about itself — the
ideal it is aiming at (`journey.md`), the walk that gets there (`phases.md`),
and the mechanisms it forced along the way, one bounded mechanism per file.
Reading a project end to end is `ls` and then reading in order; adding a doc to
one edits nothing outside it.

The rule for this directory: **a doc lives with the work that forced it.** A
design that opens by naming the debt it discharges belongs beside the doc that
owes the debt, not in a pile of designs sorted by nothing. What stays at the
top level of `docs/` is what belongs to no single project — `architecture.md`
(one physical map of one system, moved by every project and owned by none), the
guides, and the records indexed by time rather than by subject: `changelog/`,
`research/`, `reviews/`.

A project with one design names it `design.md`; a project with several names
each for what it designs.

**Where each project stands now lives in the project's own primary doc**, in
front matter, and [`../ROADMAP.md`](../ROADMAP.md) is generated from it — so
the roadmap cannot disagree with the thing it describes. The third column below
is prose for a reader; the machine-readable answer is the front matter, and
`node scripts/roadmap.mjs` is what turns it into a view.

| Project | What it is | Where it stands |
| --- | --- | --- |
| [multiuser](multiuser/) | The hosted, multi-user build: sharing a canvas, the identity desk, the innkeeper, homes and replicas. [`journey.md`](multiuser/journey.md) is the ideal and the acceptance suite; [`phases.md`](multiuser/phases.md) is the walk, and its "where we are" line is the one to read first. | **Phase 14 closed, 25 Aug 2026 — isocan.io is live; phase 10.5 closed 29 Aug when Paul and Dion walked `development.md`.** Nothing on this project is owed to anybody now, and the feature list shrank to one: phases 12.5 and 12.7 — the queue and the local bridge — were retired 30 Aug because an agent cannot work with no network to reach a model through, and phases 12 and 13 followed the same day (agent-on-demand moved out to [on-demand](on-demand/); offline birth shared the airplane's premise, leaving re-homing and the twin rule to headings of their own). Phase 11 — the thin agent — closed later that same day, Scene 6 played for real in a cloud workspace against dev. **Every phase is now closed or retired.** |
| [ui-refresh](ui-refresh/) | The canvas as a stage rather than a workbench with tools bolted to the walls — a floating rail, results drawn beside the message that made them, and chrome that arrives when summoned. [`phases.md`](ui-refresh/phases.md) is the walk; the design itself lives on canvas `prj_trml8m4Zfh` (seven screens, a rationale, and a spec for two of them). | **Phases 1–6 built, 28 Aug 2026.** The rail floats and opening it PANS the canvas; shut, it is a 48px strip carrying unread and the agents that are working. The lane draws what a message made, with a tether that refuses to lie. The header dissolved into floating clusters, eight bar controls became three behind a `···`, and the agent tray gives `isocan who` a home. Full screen presents (chrome rests while you flip slides) and the workbench joins the same language. **All three harness-blocked acceptances were closed by hand on 29 Aug** — ⌘J reaches the page, the blur is fine under a pan, and follow flies. They had waited on one fact: the harness delivers no key events, never runs a frame, and reports the tab hidden. |
| [context](context/) | What an agent actually reads when it starts work here — seeing it, managing it, and whether an external memory system (Honcho, Hindsight) belongs. [`design.md`](context/design.md) settles the last one first: the canvas is the RECORD and a memory system is at most an INDEX, held to three tests — can it be undone, can everyone see it, does it work offline. | **Stage 1 built, 29 Aug 2026.** `isocan context` and a fourth dock panel, both reading one `contextPieces`. Stores nothing: every number is counted when you ask. Stale is measured against the work rather than the clock, and always carries a reason. |
| [mindmap](mindmap/) | Riffing with an agent into a mind map you can drag: real nodes, real arrows, on the canvas. [`design.md`](mindmap/design.md) records a reversal — the first draft argued against a graph by weighing it as a new op type, when the nodes already exist as text items and an edge is a property. Direct manipulation is what settles it: a dragged node needs its position to be a canvas fact. | **Stages 1, 2 and 4 built, 29 Aug 2026.** `map new/add/link/show/ls` on the CLI, lines drawn on the canvas, zero new op types. Layout is partial on purpose — children stack beside their parent, which is legible; a balanced tree waits until somebody has dragged a real map into the shape it should have had. |
| [launch](launch/) | What happens when an agent-on-demand hook actually fires — the operational half of frozen delegation, which [innkeeper.md](multiuser/innkeeper.md) left as a slogan. | **Superseded 30 Aug 2026** — the ACP address hook of [on-demand](on-demand/) is now the authoritative wake shape. Kept as the measured record of the dispatch alternative: the spike ran and answered four of its five guesses; the fifth (whether `actions:write` without `actions:read` can dispatch) is moot unless a dispatch hook returns. |
| [on-demand](on-demand/) | An agent is not a process, and isocan currently requires it to be one — `--launch` opens a terminal tab per agent. [`design.md`](on-demand/design.md): **`isocan rc`** — a long-running command the user starts, the shape of `claude rc`, with `isocan agent` as its vocabulary twin for what agents type — holds the enrolments, parks against the home the way `wait` does, and vends agent sessions over ACP stdio when an op arrives. A session becomes an instance rather than an identity, and no agent is ever spawned by machinery nobody launched. The first version runs entirely on one machine, with no service and no account. | **Designed 30 Aug 2026, unbuilt; mechanism revised once in review the same day** (self-enrolment via `ISOCAN_HOOK`/exit 3 withdrawn — `isocan rc` replaced it; `isocan wait` no longer changes at all). The load-bearing measurement stands: a park costs a model turn per timeout expiry — about fifty inferences across a quiet night at the guide's own `--timeout 570` — and a park is a process, so nothing survives its death. Phase 1 (durable cursors) is worth building alone. **Authoritative since 30 Aug**: the ACP address shape won the hook decision — a dispatch creates an agent per summons with weak observability, an address wakes one that exists with exact observability — and [launch](launch/) is superseded, kept as the measured record of the dispatch alternative. |
| [atlas](atlas/) | Understanding a system you did not write, on a canvas, with an agent — and keeping that understanding current as the system moves. [`journey.md`](atlas/journey.md) plus the two mechanisms it forced: [convergence](atlas/convergence.md) (the canvas can diverge and cannot converge) and [the content origin](atlas/content-origin.md) (serve item content from an origin that owns nothing). | Journey written 24 Aug 2026. Both mechanisms designed, neither built. **Not to be confused with `isocan choose`**, which shipped 29 Aug: that converges a DESIGN exploration onto its source, while atlas's convergence is about a canvas that has diverged from the system it describes. Same word, different problem. The content origin now has an [execution plan](atlas/content-origin-plan.md) (26 Aug): four stages, local half first, each stage stable on both shapes. |
| [personas](personas/) | A named role an agent takes on — a lens, skills, a goal it is judged against, and a memory of what it already found. [`README.md`](personas/README.md) is the feature set; [`design.md`](personas/design.md) is the argument, which starts from the observation that this repo had been doing it by hand for a week. | **Steps 1–5 built, 29–30 Aug 2026.** Seven personas in `.agents/personas/` with doorways into `.claude/agents/`; goals that are `(number, bound, command)` with measured baselines; `isocan persona ls / show / runs`; a nightly that writes a dated page and changes nothing else. Step 6 (the canvas as source of truth) is gated on a second person editing a persona. The load-bearing rule: **a persona's memory is the guards it wrote, not the notes it left.** |
| [evals](evals/) | Finding out whether isocan is any good at the thing it exists for, and then continuing to find out. [`plan.md`](evals/plan.md) is staged: observe, then measure, then judge, then intervene. | Staged plan. Stage 2's graders exist in [`scripts/grade.mjs`](../../scripts/grade.mjs) and are wired to nothing — which the night-shift research found by typing the command nobody types. |
| [workbench](workbench/) | A second top-level view that flips from the canvas: agents \| files \| artifact \| editor. Talk to every agent by @name, see what each is doing right now. | **Built and shipping** — the route, the agent column, the directory pane and the thread are live, and the room was restyled into the refresh's language on 29 Aug. The doc still reads as a proposal ("not chosen") because it was written as one; it is now a record of why the room is shaped as it is, and the parts it proposed that did NOT ship are the honest thing to extract from it next. |
| [extensions](extensions/) | Making things *of* the canvas rather than *on* it: a new entry in the tool rail, a panel down the side, a view that did not ship. | Designed. The 24 Aug architecture review judged the model correct — `role=tool` is invariant 3 exactly, no new operation — and named its gate: `does` needs runtime validation before tier 3 is safe. Not built. |
| [auto-upgrade](auto-upgrade/) | A CLI that catches up with its home, in a project that ships several times a day. [`design.md`](auto-upgrade/design.md) is the argument, [`phases.md`](auto-upgrade/phases.md) the walk — four phases, the first two notify-only — and [`journey.md`](auto-upgrade/journey.md) the acceptance suite, written after the design; writing it surfaced eight design gaps, recorded at its end. | **All four phases done, 27–29 Aug 2026.** isocan.io can say which commit it is running; a CLI that disagrees with its home says so once per pair of shas, naming both builds and the home. An upgrade is now a build installed aside into `~/.isocan/builds/<sha>`, started and asked `/healthz` which commit it is, and `current` flipped only if it answered correctly — with `--rollback` and `--pin` as the way back. On a managed install that happens while an agent is parked, reported in the wake it was parked for. Three things stay open and are named at the top of `phases.md`: the wake cannot say what changed beyond the sha (the image has no `.git`), a fresh machine still starts unmanaged because the front door runs `npm i -g`, and the trust boundary — whatever is on `release` now runs unattended on every managed machine — has not been met as a decision. |

**That project is no longer owed.** [`launch/design.md`](launch/design.md) was
written 29 Aug 2026 and owns the operational half of frozen delegation: the
hook contract as an actual request, pass-redemption-within-a-deadline as the
only failure signal a write-only token can have, the payload as a channel that
is not private, the harness credential that lives outside the walls, the
second death by vendor token expiry, and the re-run button that can never
work. It ends with five named guesses for the spike to measure, because a
design that reasons about a vendor is a hypothesis.
