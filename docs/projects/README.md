# Projects

One directory per body of work. A project holds everything about itself — the
ideal it is aiming at (`journey.md`), the walk that gets there (`phases.md`),
and the mechanisms it forced along the way, one bounded mechanism per file.
Reading a project end to end is `ls` and then reading in order; adding a doc to
one edits nothing outside it.

The rule for this directory: **a doc lives with the work that forced it.** A
design that opens by naming the debt it discharges belongs beside the doc that
owes the debt, not in a pile of designs sorted by nothing. What stays at the
top level of `docs/` is what belongs to no single project — `architecture.md`
(one physical map of one system, moved by every project and owned by none), the
guides, and the records indexed by time rather than by subject: `changelog/`,
`research/`, `reviews/`.

A project with one design names it `design.md`; a project with several names
each for what it designs.

**Where each project stands now lives in the project's own primary doc**, in
front matter, and [`../ROADMAP.md`](../ROADMAP.md) is generated from it — so
the roadmap cannot disagree with the thing it describes. The third column below
is prose for a reader; the machine-readable answer is the front matter, and
`node scripts/roadmap.mjs` is what turns it into a view.

| Project | What it is | Where it stands |
| --- | --- | --- |
| [webmcp-cobrowsing](webmcp-cobrowsing/) | The canvas as WebMCP tools: a browser agent and a human sharing one live session through the page’s own Operation vocabulary. [journey.md](webmcp-cobrowsing/journey.md) registers the project thin; design and phases follow the census-informed design. | **Roadmap defined 11 Sep 2026 (docs-only).** Extends [@isocan/mcp](../../packages/mcp/)’s read tools toward co-presence; the WebMCP API is verified experimental (Chrome 149 origin trial). Links [embed](embed/) and [iso-api](iso-api/). |
| [multiuser](multiuser/) | The hosted, multi-user build: sharing a canvas, the identity desk, the innkeeper, homes and replicas. [`journey.md`](multiuser/journey.md) is the ideal and the acceptance suite; [`phases.md`](multiuser/phases.md) is the walk, and its "where we are" line is the one to read first. | **Phase 14 closed, 25 Aug 2026 — isocan.io is live; phase 10.5 closed 29 Aug when Paul and Dion walked `development.md`.** Nothing on this project is owed to anybody now, and the feature list shrank to one: phases 12.5 and 12.7 — the queue and the local bridge — were retired 30 Aug because an agent cannot work with no network to reach a model through, and phases 12 and 13 followed the same day (agent-on-demand moved out to [on-demand](on-demand/); offline birth shared the airplane's premise, leaving re-homing and the twin rule to headings of their own). Phase 11 — the thin agent — closed later that same day, Scene 6 played for real in a cloud workspace against dev. **Every phase is now closed or retired.** |
| [agent-custody](agent-custody/) | Who answers for an enrolled agent, and how the home knows. [`design.md`](agent-custody/design.md) pairs #83 (a web-minted agent's face is dropped at the home — no badge that runs it can vouch for it) with #81 (rc liveness never relays, so the Web UI reads every agent as unanswered). | **Designed and built 31 Aug 2026.** The call: no "＋ Add an agent" in the Web UI until the home can see a parked rc; with one visible, enrolment is a handshake the rc completes — the ask rides the hold's own reply, the actor is born first-claim on the machine that answers for it, and the desk changed not at all. Rc liveness now relays up the home-link beside the faces, connection-bound at every hop. Its oldest open question — whose ask a parked rc honors, the one the modules design names as its "compute consent" gate — was **answered 11 Sep 2026: its owner's**, by owner-only summons. Still open: the dialog naming the machine it promises. |
| [standing-agents](standing-agents/) | One agent, one name, watching several canvases, woken into the one that wants it. [`design.md`](standing-agents/design.md) starts from what on-demand left: the rc was one canvas wide three times over — the directory's binding, an enrolment key with a canvas in it, and a summons that handed the agent no canvas — so "Percy on a second canvas" was refused by the desk's name rule, #89 in another coat. [`phases.md`](standing-agents/phases.md) is the walk. | **All four phases closed 3 Sep 2026.** Phase 1 (1 Sep): the enrolment key is the name, so the same name enrolled on another canvas from the same machine is the same actor; the summons carries `ISOCAN_CANVAS`, read like `--canvas`. Phases 2–4 (3 Sep): `isocan rc --all` parks on every canvas its rows name, `available` in the web roster, and `isocan history <actor>`. The cross-machine half stays a vouch on purpose. |
| [embed](embed/) | isocan inside somebody else's window — an agent manager's pane, an IDE panel, a host that renders it. [`phases.md`](embed/phases.md) is the walk; the research it stands on is [the 9 Sep note](../research/2026-09-09-agent-managers.md), whose finding is that the embed was already working and had no identity. | **Phase 1 built 9 Sep 2026.** The gate was never framing — the hosted home sends no `frame-ancestors` — it was admission: a `SameSite=Lax` cookie with no `Partitioned`, an app with no bearer path, and therefore a stranger on every load, which is a mint on every load, which is the door's meter. Now a framed request over TLS gets `SameSite=None; Secure; Partitioned`, so the pane keeps its own badge, isolated from the person's own tab; the page load sniffs `Sec-Fetch-Dest`, the door is told by the app, because only one of the two mint paths can be asked. `isocan embed` is the third act beside `share` (a person) and `pass` (a machine): an address for a WINDOW. Local daemons over plain HTTP keep today's behaviour and say so — `Partitioned` requires `Secure`. Phase 2's read half was built the same day — `@isocan/mcp`, six read tools, `isocan mcp` as the command a manager spawns — and settled the decision it was gated on: an MCP client is whoever the machine already is, the person by default. Its write half waits on addressability; phase 3 (MCP Apps) is unbuilt. |
| [harnesses](harnesses/) | Which coding harness an agent runs in — pi beside Claude Code, and what a machine does when nobody named one. [`journey.md`](harnesses/journey.md) is the whole project: two journeys and the decisions they forced. | Built 4 Sep 2026. |
| [multi-identity](multi-identity/) | Being the same person on more than one machine. [`design.md`](multi-identity/design.md) starts from the debt: the identity desk's mechanism 6 (person resumption across browsers) was built in phase 9 and played on prod, but no surface reaches it — the door refuses the taken name in prose while rendering no control that proves an address. The design: `resumable` rows render exactly as known-identity rows, in the same place, because to the person they are the same act. [`journey.md`](multi-identity/journey.md) is the acceptance suite — six journeys, ordered from the main path outward; journey 5 verifies nothing changed for anyone else, and journey 6 is the person who already became a second name — and [`phases.md`](multi-identity/phases.md) the walk: five phases, each closing a journey by walking it. Phases 1–4 are the door and the menu, all in `packages/web` save one corrected menu label; phase 5 is the project's one new op, `actor.join`. The multiuser docs it leans on are records; this is where the design moves. | **Built 1 Sep 2026, all five phases closed the day after the design.** Scope as decided: the door — four states plus D′ (proved, nobody to be), a `name-taken` refusal that renders its remedy as a control, and the rule that while the door is showing, the door owns the offer. The precondition (the first machine must have proved the address) stays copy — `VerifyDialog` inverts to lead with resumption. Phases 1–4: no server change, no new op, no new route. Phase 5 (added 1 Sep): `actor.join` folds a second actor into the person who holds both, a registry op in the family of colors and marks, so readers resolve the id and the log is never rewritten. Left out on purpose: the device-handoff pass ("Open on another device…"), a different gesture that belongs to this project as later work. |
| [ui-refresh](ui-refresh/) | The canvas as a stage rather than a workbench with tools bolted to the walls — a floating rail, results drawn beside the message that made them, and chrome that arrives when summoned. [`phases.md`](ui-refresh/phases.md) is the walk; the design itself lives on canvas [\[isocan\] UI](https://isocan.io/p/prj_trml8m4Zfh) (seven screens, a rationale, and a spec for two of them). | **Phases 1–6 built, 28 Aug 2026.** The rail floats and opening it PANS the canvas; shut, it is a 48px strip carrying unread and the agents that are working. The lane draws what a message made, with a tether that refuses to lie. The header dissolved into floating clusters, eight bar controls became three behind a `···`, and the agent tray gives `isocan who` a home. Full screen presents (chrome rests while you flip slides) and the workbench joins the same language. **All three harness-blocked acceptances were closed by hand on 29 Aug** — ⌘J reaches the page, the blur is fine under a pan, and follow flies. They had waited on one fact: the harness delivers no key events, never runs a frame, and reports the tab hidden. |
| [context](context/) | What an agent actually reads when it starts work here — seeing it, managing it, and whether an external memory system (Honcho, Hindsight) belongs. [`design.md`](context/design.md) settles the last one first: the canvas is the RECORD and a memory system is at most an INDEX, held to three tests — can it be undone, can everyone see it, does it work offline. | **Stages 1 and 2 built, 29–30 Aug 2026.** `isocan context` and a fourth dock panel, both reading one `contextPieces`. Stores nothing: every number is counted when you ask. Stale is measured against the work rather than the clock, and always carries a reason. Stage 3's MCP surface arrived 9 Sep from [embed](embed/) — `@isocan/mcp`, six read tools — without this stage's own list: `read_context` and MCP resources are what is missing. |
| [iso-api](iso-api/) | The third isomorphic surface (#78): scripting the canvas from code, no process per action. [`design.md`](iso-api/design.md)'s claim: the API already exists as the CLI's private middle layer — `DaemonClient`, canvas/identity resolution, the home walk — and the work is a seam: move it to `packages/api`, make the CLI its first consumer. [`journey.md`](iso-api/journey.md) is the acceptance suite, written as jobs to be done; the first hirer is `scripts/canvas-board.mjs`, whose 10–25 spawns per run are the requirements list written in workarounds — and whose job generalizes: dashboards and mini apps that live on the canvas, updated by re-push (the live version belongs to [extensions](extensions/)). | **Built 31 Aug 2026 — designed and all four phases closed the same day**, every journey played for real. Doors settled: typed library with the CLI atop it, release-branch install, no in-process engine — the API stays a client of the daemon. Journey 1 (the board, ported) settled `connect()`'s surface; journey 4 (a stranger's machine) installed from the release branch and landed an op at dev as its own actor. What a harness-less script gets for an identity was settled 9 Sep by [embed](embed/)'s MCP server: the machine's person, by default. Open: whether park/rc exports wait for [on-demand](on-demand/). |
| [mindmap](mindmap/) | Riffing with an agent into a mind map you can drag: real nodes, real arrows, on the canvas. [`design.md`](mindmap/design.md) records a reversal — the first draft argued against a graph by weighing it as a new op type, when the nodes already exist as text items and an edge is a property. Direct manipulation is what settles it: a dragged node needs its position to be a canvas fact. | **Stages 1, 2 and 4 built, 29 Aug 2026.** `map new/add/link/show/ls` on the CLI, lines drawn on the canvas, zero new op types. Layout is partial on purpose — children stack beside their parent, which is legible; a balanced tree waits until somebody has dragged a real map into the shape it should have had. |
| [launch](launch/) | What happens when an agent-on-demand hook actually fires — the operational half of frozen delegation, which [innkeeper.md](multiuser/innkeeper.md) left as a slogan. | **Superseded 30 Aug 2026** — the ACP address hook of [on-demand](on-demand/) is now the authoritative wake shape. Kept as the measured record of the dispatch alternative: the spike ran and answered four of its five guesses; the fifth (whether `actions:write` without `actions:read` can dispatch) is moot unless a dispatch hook returns. |
| [on-demand](on-demand/) | An agent is not a process, and isocan currently requires it to be one — `--launch` opens a terminal tab per agent. [`design.md`](on-demand/design.md): **`isocan rc`** — a long-running command the user starts, the shape of `claude rc`, with `isocan agent` as its vocabulary twin for what agents type — holds the enrolments, parks against the home the way `wait` does, and vends agent sessions over ACP stdio when an op arrives. A session becomes an instance rather than an identity, and no agent is ever spawned by machinery nobody launched. The first version runs entirely on one machine, with no service and no account. | **Built 30 Aug 2026 — every phase closed in one day, the design revised once in review before the walk began** (self-enrolment via `ISOCAN_HOOK`/exit 3 withdrawn — `isocan rc` replaced it; `isocan wait` changed not at all). The park's cursor is durable; enrolment is a record with verbs in both spellings; the ACP client speaks to real adapters with sessions that resume; a summoned agent replied in a thread with presence; the limits hold with every stop spoken by the SYSTEM voice; `isocan who` tells answerable from running from gone. Journey 10 is lived rather than built. **Authoritative for the wake since 30 Aug** — an address wakes an agent that exists, and [launch](launch/) is superseded as the measured record of the dispatch alternative. Residue: personas, and the compute-consent question [agent-custody](agent-custody/) names. |
| [atlas](atlas/) | Understanding a system you did not write, on a canvas, with an agent — and keeping that understanding current as the system moves. [`journey.md`](atlas/journey.md) plus the two mechanisms it forced: [convergence](atlas/convergence.md) (the canvas can diverge and cannot converge) and [the content origin](atlas/content-origin.md) (serve item content from an origin that owns nothing). | Journey written 24 Aug 2026. **Both mechanisms built, convergence only in the terminal.** Convergence shipped 29 Aug as **`isocan choose`** — the verb Scene 8 and [convergence.md](atlas/convergence.md) both name; this row said until 11 Sep that `choose` was a different convergence, and it is not. It departs from the design (one grouped gesture instead of a new op; the losers go to the trash rather than onto the stack) and has no web "Keep this" door. The content origin's [execution plan](atlas/content-origin-plan.md) (26 Aug) is **closed, 6 Sep**: all four stages built — the role, the local second listener, the CSP chosen by measuring 76 real screens, and the hosted half on isocan.store with short-lived signed reads ([the decision](multiuser/content-read-auth.md)). What the scenes still force: the question as a thread property, and Scene 7's rebuild loop with a structural fingerprint — keeping an atlas current as its system moves, which is the problem the old row mistook for convergence. |
| [personas](personas/) | A named role an agent takes on — a lens, skills, a goal it is judged against, and a memory of what it already found. [`README.md`](personas/README.md) is the feature set; [`design.md`](personas/design.md) is the argument, which starts from the observation that this repo had been doing it by hand for a week. | **Steps 1–5 built, 29–30 Aug 2026.** Nine personas in `.agents/personas/` (seven on 29 Aug; `reviewer` and `journeys` joined on the 30th) with doorways into `.claude/agents/`; goals that are `(number, bound, command)` with measured baselines; `isocan persona ls / show / runs`; a nightly that writes a dated page and changes nothing else. Step 6 (the canvas as source of truth) is gated on a second person editing a persona. The load-bearing rule: **a persona's memory is the guards it wrote, not the notes it left.** |
| [sprint](sprint/) | `/sprint` as a whole flow rather than a clock: the facilitator lays out a **board** — one area per phase, in the order the week runs — and walks the room across it, each area's card saying what happens there and the chip offering the one action the phase wants. [`journey.md`](sprint/journey.md) is the ideal, seven scenes from `/sprint <brief>` to rival rooms; [`phases.md`](sprint/phases.md) is the walk, and phase 0 — *areas*, a titled region things are placed in, membership derived by geometry — is the one primitive everything else stands on. Builds on the 1 Sep research's stages 0–2 (clock, curtain, tally), which stay as they are. | **Phases 0–5 built 2 Sep 2026, the same day as the design; phase 6 rehearsed over the wire.** Areas on both surfaces (`core/area.ts`, `isocan area`, `--in`); the board as data both surfaces lay, the brief as one card with a history; the walk — a phase call glides everyone who watched it to the sheet, the chip offers *Go there*, *New note*, *Hand in*; desks — a private canvas per sketcher, link off, one pass, its chip reading the sprint's clock and handing in across canvases; the wall as the Vote sheet with the curtain there only, and the heat map as dots placed on the part of a sketch (`item.react` carrying a point — the project's one wire change); grids with named rows and columns, cells you can address, the deck from a sheet. `cli/test/one-hour.test.ts` plays the one-hour cut end to end against a daemon. Still owed: the run with people the note asked for first. |
| [memory](memory/) | Memory in layers you can see: this canvas, the canvases it links (a design system four products inherit), and the person's own `~name` canvas, born private and linked or unlinked from any canvas by one visible item. Every layer is an ordinary canvas and therefore a directory of files — which is why it can be a git repo (`export --git`), a synced folder, or local only — shown in one Context view with a heading per source and a *from* chip on every borrowed piece. Honcho stays an index over MCP, never the record. [`design.md`](memory/design.md) is the design, four scenes, five phases. | **Designed 2 Sep 2026, unbuilt.** Builds on [context](context/) (the rule and the three tests) and [inception](inception/) phase 0 (the link is a canvas item). Zero new op types: two property values and a canvas born the way desks are. Phase 0 — the Context view with headings — is next. |
| [inception](inception/) | A canvas placed on a canvas: an ordinary item whose blob is the other canvas's address, drawn live and small from its snapshot the way the lens draws items small, opening in a tab on a double-click or its ↗, with a real screenshot as the fallback picture rather than the mechanism. One popup with two doors — search your canvases, or paste an address — and `isocan canvas place` as the terminal's twin, so an agent can shelve a person's canvases onto sheets. [`design.md`](inception/design.md) is the design, the four scenes and the five phases. | **Designed 2 Sep 2026, unbuilt.** Zero new op types is the target; one property says which canvas. Phase 0 — the item and the card, placed from the terminal — is next. |
| [evals](evals/) | Finding out whether isocan is any good at the thing it exists for, and then continuing to find out. [`plan.md`](evals/plan.md) is staged: observe, then measure, then judge, then intervene. | Staged plan. Stage 2's graders exist in [`scripts/grade.mjs`](../../scripts/grade.mjs) and run nightly (`.github/workflows/grade.yml`) — wired after the night-shift research found them reachable by no command anybody typed. |
| [workbench](workbench/) | A second top-level view that flips from the canvas: agents \| files \| artifact \| editor. Talk to every agent by @name, see what each is doing right now. | **Built and shipping** — the route, the agent column, the directory pane and the thread are live, and the room was restyled into the refresh's language on 29 Aug. The doc still reads as a proposal ("not chosen") because it was written as one; it is now a record of why the room is shaped as it is, and the parts it proposed that did NOT ship are the honest thing to extract from it next. |
| [modules](modules/) | A package that contributes to both surfaces at once and can be removed leaving every item it made readable as a file. [`design.md`](modules/design.md) turns the research note's counts into a contract — a `CoreModule` record in core's registry, the shell's slots fed facts as props, the CLI's host handing a module its own helpers, two lists as the whole coupling — and settles how runtime loading works before it is built. [`phases.md`](modules/phases.md) is the walk; [`authoring.md`](modules/authoring.md) is the manual for writing one — the layout, every extension point and its shape, the host, the build and install, the guards. | **Phases 1–4 built 4–5 Sep 2026** — the registries; the mind map as the first internal module; Mermaid as the first node-type module; runtime loading (`isocan module add/rm/ls`, served per request, loaded through a host object, no restart, no import map, now from a git spec too); and documents, which brought the inspector, page and command slots. Phase 5's three gates were read on 12 Sep: the content origin closed 6 Sep, compute consent was agent-custody's "whose ask a parked rc honors" and closed 11 Sep, and extension actors turns out to bind only the BROWSER-frame sandbox. So the agent-side half was built the same day — `@isocan/sandbox`, a program that is an ordinary file on the canvas and runs fenced on the machine that typed `isocan sandbox run`, through the promoted `CliHost.runFenced` that has no unfenced path. The frame half stays gated behind extensions stages 3-4 and a CSP line. |
| [extensions](extensions/) | Making things *of* the canvas rather than *on* it: a new entry in the tool rail, a panel down the side, a view that did not ship. | **Stages 1 and 2 built 6 Sep 2026** — a declarative tool as an item with `role=tool`, `does` limited to a slash command that exists, and `isocan tool list/add` and the rail reading from one reader in core. The 24 Aug architecture review judged the model correct — `role=tool` is invariant 3 exactly, no new operation. Stages 3–5 (declarative panels, extension actors, hosted panels) are not built. |
| [auto-upgrade](auto-upgrade/) | A CLI that catches up with its home, in a project that ships several times a day. [`design.md`](auto-upgrade/design.md) is the argument, [`phases.md`](auto-upgrade/phases.md) the walk — four phases, the first two notify-only — and [`journey.md`](auto-upgrade/journey.md) the acceptance suite, written after the design; writing it surfaced eight design gaps, recorded at its end. | **All four phases done, 27–29 Aug 2026.** isocan.io can say which commit it is running; a CLI that disagrees with its home says so once per pair of shas, naming both builds and the home. An upgrade is now a build installed aside into `~/.isocan/builds/<sha>`, started and asked `/healthz` which commit it is, and `current` flipped only if it answered correctly — with `--rollback` and `--pin` as the way back. On a managed install that happens while an agent is parked, reported in the wake it was parked for. Three things stay open and are named at the top of `phases.md`: the wake cannot say what changed beyond the sha (the image has no `.git`), a fresh machine still starts unmanaged because the front door runs `npm i -g`, and the trust boundary — whatever is on `release` now runs unattended on every managed machine — has not been met as a decision. |
| [roles](roles/) | Who may do what on a canvas, and on a set of canvases, and how that changes over time. [`journey.md`](roles/journey.md) is the acceptance suite: eight journeys, from locking one canvas to the people you name, through a **space** (a named set of canvases) locked to a **group** (a named set of people), to the creator as an unremovable floor under grantable ownership. Starts from the 1 Sep roles research and adds the two things it did not ask for, the space and the group. | **Built 2–3 Sep 2026** — the ladder in core (`atLeast`, `narrowed`), the creator's floor at the door, the `read` rung stored and carried on every wire, the read-only canvas held by a test that walks the hidden-writes list, `own` grantable with every grant write owner-gated, the sweep reporting per badge so a raise reaches an open tab as `standing` and a removal as `withdrawn`, the bar (**Remove** says when the link would still admit them and offers **and keep them out**), the space: a desk row naming a set of canvases, its grants read at every door beside the canvas's with the highest rung winning, `isocan space` and `share --space`, headings on the canvas list; and the group: a `group:` subject whose members are read at the door and never copied, so removing one reaches every canvas the group opens in one write, `isocan group` and a **Groups…** panel; journeys 1 to 7 play; journey 8 walked on isocan.io on 3 Sep after the deploy — pre-deploy rows with no rung still mean edit, a pre-deploy `view` link still opens the deck, no heading with no space — and against a pre-phase-1 build, where an old CLI reads a `read` link as an ordinary link and is refused writes with `view-only`. **All six phases closed.** [`design.md`](roles/design.md): the four-rung ladder compared in one place; the creator as the floor and `own` as a row; a grant scoped to a canvas or a space, both read at the door, highest rung wins; a `group:` subject whose membership is read at the door, never copied; a bar as a row that beats every rung; the sweep as the one recompute, reporting per badge so a rung change reaches an open socket. [`phases.md`](roles/phases.md): six phases, one per journey pair, the read-only canvas and the space as the boulders. |
| [switcher](switcher/) | Going to another canvas from wherever you are, without the trip through the home screen. [`design.md`](switcher/design.md) is the research it stood on (what every app with several documents converged on), the decisions phase 1 made (one window with two faces, three doors, lately first, fuzzy here and only here, a move rather than a cut, no CLI verb) and the walk. | **Phase 1 built 4 Sep 2026** — ⌘O, ⌘K → Switch canvas…, and the caret beside the name. Phases 2–5 (recents on the home screen and the lens, spaces as headings, a shared "lately", the project › canvas row) are open and followed in #134. |
| [sheep-harness](sheep-harness/) | An agent whose sessions run in a cell at a [sheep](https://github.com/dglazkov/sheep) home instead of a subprocess on the laptop. `--harness sheep` on an enrolment; the rc, its routing and its custody rule unchanged; identity by an `isocan pass` minted for the agent's actor and redeemed once by the pasture's setup script. [`design.md`](sheep-harness/design.md) is the argument, [`journey.md`](sheep-harness/journey.md) the acceptance suite, [`phases.md`](sheep-harness/phases.md) the walk. | **Closed 11 Sep 2026, at phase 2.5.** `sheep` is found by `isocan harness` like the others, its home is carried on the rc row, a birth spends no model turn and gives the pass to the sheep alone, and withdrawal ends the sheep and the badge its cell redeemed. Journeys 1, 2 and 4 walked on the deployed station and a local sheep home against dev.isocan.io. Phase 3 (a week with the bill) was retired unstarted, since a week of use is use. The install on every cold turn stays until sheep#2 lands. |
| [operator](operator/) | What the person who runs a home can do to things they do not belong to — take a canvas down, end a badge, turn off a grant, refuse somebody at the door — which `/terms` promises and no route does ([#77](https://github.com/dglazkov/isocan/issues/77)). [`journey.md`](operator/journey.md) is the acceptance suite, twelve journeys in the order a real report needs them; [`design.md`](operator/design.md) the argument; [`phases.md`](operator/phases.md) the walk. | **Phases 1 and 2 PART-DONE 12 Sep 2026; designed 11 Sep.** The operator is a proof, not a credential: an address `ISOCAN_OPERATORS` names, proved by the home's own attester within ten minutes, carried with each act and stored nowhere — so no badge holds operator standing and no agent can borrow it. Acts run in the daemon over HTTP and are desk records, never ops. Taking down is not deleting: the home stops serving, replicas keep their copies, and it lifts until a separate purge. The kill gaps found while mapping — open sockets, a dead minter's passes, a CLI that re-badges and resumes — are fixed for everyone. Phase 1 is built and green here — the proof carried in one header beside the badge, refusals that name the address they proved, the ledger written before every answer, `isocan operator show/log`, and the verb asking the home whether it has an operator before it opens a browser — but not walked. Phase 2 the same day: a look that admits for an hour and is not in presence, and a takedown that stops the home serving a canvas — refused everywhere with the sentence, never a 404; replicas keep their copies; it lifts — walked by hand against a local daemon. The dev walks for both wait on a person: the list on dev, a sign-in, a second account, a third browser. The terms wording meanwhile is Dion's call; isocan.io's list and eight posture questions are Dimitri's. Phase 3 (purge) is next, and needs none of it. |
| [design-competition](design-competition/) | *Choose your fighter*: pick famous designers from a picker, give them one brief, and watch an agent per designer build a rival design in its own lane before the room votes. Built as `@isocan/design-competition` on purpose — the module system's hardest test. [`journey.md`](design-competition/journey.md) is the ideal, eight scenes; [`design.md`](design-competition/design.md) the argument; [`packs.md`](design-competition/packs.md) the fighter format and the nine defaults; [`module-gaps.md`](design-competition/module-gaps.md) the six platform changes it forces; [`phases.md`](design-competition/phases.md) the walk. Epic [#250](https://github.com/dglazkov/isocan/issues/250). | **Designed 11 Sep 2026, unbuilt.** A sprint with one phase, cast by a picker: zero new ops — areas, desks, the curtain, placed dots, the split tally and the rc's message-wake all exist. The cast is the whole cost, and every piece is a module-system gap: assets, contribution points (and data-only modules), a dialog slot, a design system scoped to an area (without which a lane's `DESIGN.md` hijacks the canvas's), enrolment templates, and a curtain outside the sprint. Homage, not impersonation — emblems not faces, and (decided 11 Sep) the principle leads and the person is a credit: *Less, but better* after Dieter Rams, an agent called *Less but Better*. Exhibition is the default bout, and it ships on isocan.io as a build-time module, lazy-loaded. Phase 0, a bout by hand measuring whether people can tell whose entry is whose, is next. |

**That project is no longer owed.** [`launch/design.md`](launch/design.md) was
written 29 Aug 2026 and owns the operational half of frozen delegation: the
hook contract as an actual request, pass-redemption-within-a-deadline as the
only failure signal a write-only token can have, the payload as a channel that
is not private, the harness credential that lives outside the walls, the
second death by vendor token expiry, and the re-run button that can never
work. It ends with five named guesses for the spike to measure, because a
design that reasons about a vendor is a hypothesis.
