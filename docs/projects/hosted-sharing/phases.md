# Hosted sharing — the walk

Each phase finishes one journey on both relevant surfaces. The shared
operation vocabulary stays unchanged; discovery and space membership belong
to the existing door and desk.

**Where we are:** phase 1 is CLOSED and phase 2 is PART-DONE, 13 September
2026. Discovery, CLI creation and built-browser submission/refusal are proved
on synthetic hosted-shaped daemons. Phase 2's native select gesture remains
to walk: injected native-selector keys were not consumed, and the later
native UI attempt found the Mac locked with no available browser surface.

## Phase 1 — Discovery honors deliberate sharing

**Status: CLOSED.** 13 September 2026 — actual HTTP across separate badges
and a built-browser home-list/known-link walk held; full tests and typecheck
passed on the integrated tree.

Integrate the hosted listing fix, preserve the local shelf, close explicit
wide discovery for link-only grants, and check adjacent public listings.

**Proof:** journey 1 with synthetic accounts through actual HTTP, including
bare, `here`, `admitted`, `admissible`, and unknown query values. Drive the
browser's list and prove known-address entry separately. Run the full suite
and workspace typecheck.

## Phase 2 — Create inside the space on both surfaces

**Status: PART-DONE.** 13 September 2026 — actual CLI and built-browser
creation, no birth link, granted-owner access and non-owner refusal held.
The browser probe selected the native option through a DOM change event;
the native selector gesture itself remains unproved.

Expose `spaceId` in CLI and web canvas creation, with ownership enforced by
the existing daemon path. Explain inherited access and retain refused intent.

**Proof:** journey 2 through the actual CLI and browser. Inspect membership
and grant rows, verify invited and unrelated accounts, and attempt creation
as a non-owner. Run the full suite and workspace typecheck.

## Verification — 13 September 2026

The conductor independently ran `npm test` (4,788 passed, 83 emulator tests
skipped), `npm run typecheck`, `npm run build`, and
`node scripts/journeys.mjs --json` (10/10). All exited 0. The CLI space tests
in that suite execute real create commands against a daemon, including a
granted space owner and an editor's refusal.

Two built-browser probes ran with `node --import tsx`: the creation walk and
an extended fresh-visitor discovery walk. The visitor initially saw no
cards, entered one known link, then saw exactly that canvas on the home
page. The private-space canvas remained absent. Creation retained the
ordinary birth link, created the selected-space canvas without one, and
retained title/selection when current ownership was lowered before submit.
Screenshots were inspected and the browser reported no errors.

The operation vocabulary is unchanged. The CLI gained `canvas create
--space` and its agent-guide entry; existing core space-name resolution is
reused. README and architecture now describe discovery and birth. HTTP,
CLI and web tests cover the authority boundary; the browser supplies the
interaction proof, with the native-select limit stated above.

## Trajectory

- **2026-09-13** — Removing link grants after selecting the winning grant
  hides valid named invitations when an older, stronger link wins entry.
  Discovery must exclude link candidates before ranking the remaining rows.
- **2026-09-13** — Narrowing the main list alone left four other address
  enumerators: homes, whereabouts, unscoped watches and takedowns. They now
  share the discovery boundary; explicit known-address reads keep the door.
- **2026-09-13** — Open: native Space selection needs an unlocked Mac.
  Pointer and keyboard injection did not exercise the native popup; the final
  CUA inventory reported a locked Mac and no app/browser surfaces. No native
  interaction followed. DOM selection plus actual submission proves the value
  and refusal behavior, not that platform gesture.
